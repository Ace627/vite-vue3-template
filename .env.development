# 仅作为标识
MODE="development"

# 后端接口代理地址
VITE_BASE_URL="http://localhost:3000"

# 后端接口公共路径
# 如无公共接口地址 直接修改代理地址即可 如有请直接将其拼于 /dev-api 之后即可 如 /dev-api/common
# 有公共接口地址的情况下，记得同时修改 .env.production 的参数值
VITE_BASE_API="/dev-api/api"
