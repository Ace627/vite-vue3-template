# 2026-08-30

## 重构项目记忆文档（MEMORY.md）
- 仅基于工作空间实际文件（package.json / vite.config.ts / src 全量 / docs / .workbuddy/skills）核对并重建 `.workbuddy/memory/MEMORY.md`，移除与现状不符的旧条目。
- 主要修正：格式化器由 oxfmt 改为 prettier（printWidth 220）；技术栈版本全量对齐 package.json（新增 echarts/@types/node/@types/lodash-es/@vue/tsconfig/fast-glob/unplugin-element-plus，移除 oxfmt）；`SystemSetting` 由 9 字段更正为 11 字段（补 showBreadcrumb/showBreadcrumbIcon）；`main.ts` 补 `setupDirectives` 步骤；`styles/index.scss` 补 transition.scss/el-dropdown.scss；补充 echarts 与 ProTable/ProPagination/ProChart/ProTooltip 二次封装（无业务消费页）；项目级 Skill 更正为 3 个（git-commit-msg 为用户级）。
- 清理：删除 30 天前的 8 篇每日日志（2026-07-20 ~ 07-27），其事实已并入重建后的 MEMORY.md。

## 二次重构：记忆文档蒸馏为 300–600 字连贯文字
- 按用户要求将 MEMORY.md 由结构化清单改写为一段 300–600 字、无分节无列表的连贯文字，所有事实已对照 package.json / vite.config.ts / src 全量 / docs / .workbuddy/skills 复核。
- 修正版本漂移：vue 3.5.41→3.5.42、vue-router 5.2.0→5.3.0、axios 1.19.0→1.20.0、@types/node 26.3.0→26.4.0（以 package.json 当前值为准）。
- 标注 README 两处过时信息：仍提 oxfmt（实际已切 Prettier，见提交 3d897fe）、项目级 Skill 列表误写 git-commit-msg（实际为 add-global-component/iterate-docs/path-alias，git-commit-msg 为用户级）。

## 修正 README 两处过时信息
- 表格与「快速开始」中的 `oxfmt 统一格式化` 改为 `Prettier 统一格式化`（对应提交 3d897fe，package.json format 脚本本就用 prettier）。
- 两处项目级 Skill 列表由 `git-commit-msg / path-alias / add-global-component` 改为 `add-global-component / iterate-docs / path-alias`，并补 `iterate-docs` 描述（文档随代码同步）；git-commit-msg 为用户级 Skill，不计入项目级。
- grep 复核 README 已无 oxfmt / git-commit-msg 残留。
