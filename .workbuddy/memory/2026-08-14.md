# 2026-08-14 工作日志

## 文档与代码一致性核查
- 核查结论：**docs 明显落后于代码**，且存在事实冲突。
- `docs/前端工程化.md` 最后更新 08-03（c379fb4），`docs/目录结构.md` 最后更新 08-12（73e81d6）；代码最新提交 08-13（5aa16b6 多标签页+路由重定向）。
- `目录结构.md` 缺失整片模块：directives/（权限指令）、Sidebar/、TagsView/、Navbar/UserDropDown+Hamburger、store permission/user/tags-view、router.helper.ts、api/auth.request.ts、types/api/user.ts、directive.d.ts、cache 三项、utils/time.ts、views/core/redirect.vue、assets 图片。
- `前端工程化.md`「未做」章节已过时：user store / 权限指令(roles) / 权限路由校验 / 侧边栏菜单 均已 mock 落地，文档仍标注为范围外/未做（仅"接真实后端"仍属范围外）。
- 缺失专题文档：侧边栏菜单、多标签页、权限路由、权限指令、用户体系/登录重构、redirect 均无对应文档。
- 处置建议：用 `iterate-docs` 技能同步（待用户确认后落地）。

## docs 同步已落地（用户确认全量更新）
- 改写 `docs/目录结构.md`：补录 directives/api/Sidebar/TagsView/SettingPanel/Navbar 子件/3 store/router.helper/types/api/user.ts/directive.d.ts/3 cache/utils/time.ts/redirect.vue/assets 图片；docs 小节补新文档名；移除已删的 views/settings。
- 改写 `docs/前端工程化.md`：B1 登录 mock 重构、B2 守卫权限分支、B3 SystemSetting 9 字段、B4 /settings 改 SettingPanel 抽屉、B5 布局含 Sidebar/TagsView/SettingPanel、B6「未做」事实冲突（user store/权限/侧边栏已 mock 落地）。
- 改写 `docs/构建与工程化/全局设置中心.md`：C1 9 字段、C2 设置 UI 改 SettingPanel、C3 showSetting 注。
- 新建 5 篇：`UI组件/侧边栏菜单.md`、`多标签页面签.md`、`权限指令.md`、`用户体系.md`、`导航栏用户菜单.md`（重建，曾删）。
- 修复 3 篇 用户体验/ 文档对旧 /settings 路由页的引用（指向 SettingPanel / Navbar ThemeSwitch）。
- 同步修正 `.workbuddy/memory/MEMORY.md` 过时长期事实（user store 已落地、/settings 移除、9 字段、权限/侧边栏/多页签已 mock）。
- 补 `iterate-docs` 技能映射表（新增 Sidebar/TagsView/权限指令/用户体系/导航栏用户菜单，修正已删 src/views/settings 旧映射）。
- 未自动 git commit（iterate-docs 约束）。
