# 2026-07-22 工作记录

- 落地本地存储封装（走法 B，纯 localStorage 包装器，去掉 Redis 命名）：
  - 改写 `src/utils/cache.util.ts`：导出 `StorageCache` 类，仅 `set/get/remove/clear` 四法；支持对象/数组（JSON）、TTL 过期（-1 永久）；统一 `app:storage:` 前缀避免与其它 localStorage 冲突；`clear()` 仅清前缀键；`set<T>` 泛型 + try/catch 返回 boolean。砍掉 Redis 专属方法（keys/exists/ttl/expire）。
  - `src/utils/index.ts` 增加 `export * from './cache.util'`。
  - 验证：`vite build` 单独跑通过（44 模块、dist 生成）；`vue-tsc -b` 因预先存在的 `useResize.ts` 引用未定义 `isMobile` 报错，与本次改动无关，已向用户说明并待确认修复。
- 用户手动修复 `isMobile` 断点：`src/store/modules/app.ts` 新增 `isMobile`（computed: device==='mobile'）与 `isDesktop` 并导出，`useResize.ts` 引用已对得上。复跑 `vue-tsc -b && vite build` 整链转绿（零类型错误、44 模块、dist 正常生成）。
- 用户重构缓存目录：将 `src/utils/cache.util.ts` 重命名为 `src/utils/storage-cache.util.ts`（内容不变，仅改文件名更表意），新增领域封装 `src/utils/cache/token.cache.ts`（基于 `StorageCache` 包装 `accessToken`/`refreshToken` 的 set/get/remove），并同步更新 `src/utils/index.ts` 导出。复跑 `vue-tsc -b` 通过（exit 0）。评审指出待修项：`get` 未传 `<string>` 导致返回 `any | null`、`setAccessToken` 未传 ttl（token 永久不过期）、缺 `clearTokens()` 登出助手——待用户确认后落地。
- ⚠️ 自我纠错：我曾在未重新读盘的情况下断言 `getAccessToken()` 返回 `any | null`，但用户早已自行把 `token.cache.ts` 改为 `StorageCache.get<string>(...)`（返回 `string | null`）并加 `: string | null` 标注、`set*` 标 `: boolean`。重新读盘确认当前文件已完全符合约定：`<string>` 类型安全已就位、无客户端 TTL、无 `clearTokens()`（用户明确不要）。教训：评代码改动必须先 re-read 磁盘现状，不能凭上一轮记忆复述。
- 给 `src/utils/cache/token.cache.ts` 补中文 JSDoc 注释：模块级头部说明 token 约定（过期归服务端 Redis / 前端不透明持久化 / 无客户端 TTL / 清场由调用方负责 / 走 `app:storage:` 前缀），各函数加一行用途注释（get 标 null 语义、remove 标登出/401 调用时机）。仅加注释，逻辑未改动。
- 用户补充 axios 请求层：`src/utils/request/index.ts`（axios.create，baseURL=VITE_BASE_API、timeout=VITE_REQUEST_TIMEOUT，注册两拦截器，导出 `request=instance`）、`interceptor/jwt-auth.ts`（请求拦截器从 `@/utils/cache/token.cache` 取 accessToken 挂 `Authorization: Bearer`，正确消费今日定好的 token 封装）、`interceptor/response-error.ts`（按 `HttpStatusCode` 映射错误文案 + `alert` 提示）。`axios@^1.18.1` 已进 dependencies。`vue-tsc -b` 通过（exit 0）。
  - 评审待修点：①🔴 401 仅提示「登录已过期」未清 token，违反 token.cache.ts 注释约定（应在 401 分支 `removeAccessToken`/`removeRefreshToken`）；②`message.substr` 已废弃且为被 `error.response.status` 覆盖的冗余死代码，应删；③`alert` 是占位，应注明后续换 toast；④`async (error:any)` 可去 async、`error` 改 `AxiosError`；⑤`.env` 缺 `VITE_BASE_API`/`VITE_REQUEST_TIMEOUT` 声明。待用户确认后落地。
- 沙箱 `pnpm` 经 corepack 路径损坏（报 `D:\c\Program Files...` 找不到 pnpm.js），改用本地二进制 `node ./node_modules/vue-tsc/bin/vue-tsc.js -b` 与 `node ./node_modules/vite/bin/vite.js build` 验证构建。
- 用户改 `response-error.ts`：①`substr` → `substring`（消废弃 API 警告）；②加 toast 占位注释「统一提示错误信息（临时用，后续换 toast）」。但仍未修核心缺口：401 分支仍只 `alert`+`reject`，未调 `removeAccessToken/removeRefreshToken`（违反 token.cache.ts 注释约定）；`async (error:any)` 仍未收紧；`.env` 仍缺 `VITE_BASE_API`/`VITE_REQUEST_TIMEOUT`。已向用户解释 `interceptors.response.use(undefined, ...)` 中 `undefined` 表示「不挂成功拦截器、2xx 原样放行」。
- 用户指出「某些情况 `error.response?.status` 不存在、但 message.substring 可截出状态码」。已核实：axios v1.x 中 "Request failed with status code XXX" 文案只在 `response` 存在时才生成（源码 settle.js 第 5 参数即 response），二者绑定；纯网络/超时/CORS 错误时 response 为 undefined、message 为 "Network Error"/"timeout..." 不含该文案，substring 分支不触发。故 line 24 的 substring 解析分支确为冗余死代码。修正后推荐写法：`const status = error.response?.status ?? -1`，既删冗余解析又显式兜住网络错误（status=-1 → 回退原始 message）。
- 用户落地了 response-error.ts 两处核心修复：`const status = error.response?.status ?? -1`（删 substring 死代码）；新增 401 分支 `if (status === Unauthorized) { removeAccessToken(); window.location.reload(); return Promise.reject(error) }`（调入 `@/utils/cache/token.cache` 的 `removeAccessToken`，满足 token 封装约定）。复跑 `vue-tsc -b` 通过（exit 0）。
  - 留待定项：②整页 reload 可改 `router.push('/login')`（模板常见取舍，非必须）；④`async (error:any)` 仍可收紧（无 await、`any` 可改 `AxiosError`）；⑥可选给 `ErrorMessageMap` 加 `-1` 项统一中文网络错误提示。均非功能性阻塞。
  - ⚠️ 自我纠错（用户当场质疑，已重新读盘核实）：a) 此前称「`.env` 缺 `VITE_BASE_API`/`VITE_REQUEST_TIMEOUT`」——错。仅基础 `.env` 缺，但 `VITE_BASE_API` 已在 `.env.development`（dev 正常）；`.env.production` 无 baseAPI 但文件注释写明「有公共接口时再补」，属有意留空。真正缺失的只有 `VITE_REQUEST_TIMEOUT`（三个 env 文件都没配 → `timeout` 恒为 0，等于全局不设超时）。b) 此前称「401 分支 `return Promise.reject(error)` 是卸载前死代码」——错。`window.location.reload()` 不同步中断 JS 执行，return 会正常执行并把 rejection 传回 axios 拦截器链；若去掉反而会让链以 undefined resolve、调用方误判请求成功。该 return 正确且必要。c) 用户明确 refreshToken 暂时不需要、仅留作扩展点，故 401 只清 accessToken 即可，此前「一并清 refreshToken」建议收回。
  - 用户手动在 `.env` 基文件补全 `VITE_REQUEST_TIMEOUT="0"`（注释：单位秒，0 表示无超时时间）。基础 `.env` 对所有模式生效，dev/prod 均继承；此前「三个 env 全无 → 靠 `|| '0'` 兜底」的隐患消除，现为显式配置。值取 0=无超时，系用户刻意选择（模板常见），合法。请求层 env 配置现已完整（VITE_BASE_API 在 dev、VITE_REQUEST_TIMEOUT 已声明）。请求层功能已完成、构建绿，剩余仅可选润色（④`async/any` 收紧、⑥`ErrorMessageMap` 加 `-1` 项、②整页 reload 改 `router.push`）。
- 清理并精简 `.workbuddy/memory/MEMORY.md`：原文件因超长被注入截断。核对磁盘实际文件后修正记忆偏差（oxfmt `semi` 实为 `false` 而非记忆写的 `true`；请求层 axios 已在 07-22 落地，旧「待办」清单已过期），删除冗余/过期段落，重绘「工程化进度」章节（已完成 / 未做 / 构建状态）。同步原文已记录的 `response-transform.ts` 拦截器与 `ApiResponse` 契约。
- 新增 vue-router meta 类型声明 + 动态标题 hook：
  - `src/types/global/vue-router.d.ts`：声明合并扩展 `RouteMeta`，含 `title?`(侧边栏/面包屑) / `icon?`(菜单图标) / `hidden?`(侧边栏隐藏)。`hidden` 为 404 兜底路由 `meta:{hidden:true}` 补的类型，消除多余属性报错。
  - `src/hooks/useDynamicTitle.ts`：`App.vue` 根组件 `<script setup>` 调用（auto-import `dirs` 含 `src/hooks`，无需手写 import）。`watch([() => route.path], …, {immediate:true})`，按 `route.meta.title` 设 `document.title`（`标题 - 应用名` 或仅 `应用名`）；`appTitle` 取 `import.meta.env.VITE_APP_TITLE`。
  - 决策：因 hook 仅在 App.vue 根组件**同步**调用 → watch 绑定 App 实例（Vue 按 `currentInstance` 归属，composable 内同步创建的 watch 等效于 setup 内直接创建）；App 不卸载 → watch 常驻（全局动态标题所需），且 Vue 卸载时自动回收无泄漏。故移除原 `onUnmounted` 内 `stopWatchDynamicTitle()`（死代码、且 Vue 本就会自动清）。`hidden?` 已补、超前注释已删。
