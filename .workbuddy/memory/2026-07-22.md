# 2026-07-22 工作日志

## 构建期 vendor chunk 分包（已落地）
- `vite.config.ts` 用 Vite 8 + Rolldown 的 `build.rolldownOptions.output.codeSplitting.groups`，将 `vue/pinia/vue-router/axios/dayjs` 拆为独立 vendor chunk，文件名 `[name]-[hash].js`（content hash）。
- 产物验证：`pinia-BwRIjB3U.js`(~59KB)、`vue-router-D9IwKmGU.js`(~22KB) 已独立成块；`vue` 实际被并入入口 `index` 块（Rolldown 对入口直接 import 的共享模块默认合入）。
- 同步把 MEMORY.md 工程化进度里「构建优化 chunk 分包」从「未做」移入「已完成」。

## 为用户分析分包收益（总结）
核心收益：①浏览器长缓存命中（vendor 不随业务发版变动，二次访问免重下~80KB 库代码）；②并发下载缩短首屏（多块并行、流式解析）；③解析/执行并行 + 失败隔离；④同类代码同块压缩率更优。
边界提醒：需服务端/CDN 对带 hash 资源配 `Cache-Control: immutable` 长缓存才生效；vue 未独立成块；业务未做路由级懒加载；碎块过多(>30)会反噬请求开销；HTTP/1.1 同域并发≈6。
- 应要求将「分包好处 + 边界」完整沉淀进 MEMORY.md「项目约定（已落地）」新增条目，便于长期参照。
- 验证 vue 去向：grep 各 chunk 发现 `createApp` 在 `index`(10.67KB,仅引用) 与 `pinia`(60.5KB,含 vue 运行时) 均出现；根因是 group `test` 正则 `/node_modules[\\/]vue[\\/]/` 未覆盖 `@vue/*` 子包，vue 运行时被 Rolldown 当未分组共享模块合并进 `pinia` 块，故无独立 `vue` 块。同步修正 MEMORY 条目描述（原「并入 index 块」不准确）。
- 用户纠正 Vite8 无 manualChunks：经查证（本地 node_modules 无 manualChunks d.ts + 官方迁移文档 `output.manualChunks object form is not supported`、函数形式亦弃用）确认。原方案 C（manualChunks 函数式）不成立已收回；正确修复是在现有 `codeSplitting.groups` 把 vue 的 `test` 扩为 `/node_modules[\\/](vue|@vue)[\\/]/`，同步写入 MEMORY 边界约束。
- 提交代码：按 git-commit-msg 流程（5 候选+两步确认），用户选 message `perf: 按依赖分包提升并发加载性能`、提交方式「提交到暂存区」；commit `1577351`（master，5 files，未推送），含 `.workbuddy/memory` 一并提交。

## 首屏加载动画（用户已落地，仅分析未改动）
- 用户新增：`index.html` 的 `#app` 内 `.app-loading` 占位 + `public/css/app-loading.css`（`<head>` `<link>` 引入）+ `main.ts` 靠 `app.mount` 自动清场。应要求分析好处，未做任何代码修改。
- 核实 Vue 3 源码（`@vue/runtime-dom@3.5.40` `app.mount`）：`if (container.nodeType === 1) container.textContent = ""`，确认 loading 节点随挂载被原生清空、自动销毁（零维护）。
- 好处：消除白屏提升感知性能 / 零 JS 依赖首屏即渲染 / `<link>` 同步注入无 FOUC（与 reset.css 一致）/ 自动销毁零维护 / 恰好覆盖 `await setupRouter` 异步死区 / 品牌一致且 `public/` 静态资源不进打包。
- 诚实提醒：①JS 彻底失败会卡死 spinner（建议 `bootstrap()` try/catch 兜底或超时降级）；②HTML `.app-loading-dots` 与 CSS `.dots` 类名对不上（死规则，动画靠 `.dot`，待清理）；③机制依赖 client mount，SSR 需换方案；④根组件需有 template 否则 Vue 会捕获 loading HTML 为模板（当前安全）。

## 清理 .dots 死规则（已落地）
- 用户要求修掉 `.dots` 死规则；复读 `public/css/app-loading.css` 发现该死规则已被（用户/linter）移除——当前文件从 `.app-loading-title` 直接到 `.dot`，无 `.dots` 段。无需改动，确认已清理。同步更新 MEMORY.md 约定④标注为已清理。

## bootstrap 失败兜底（已出方案，用户决定暂不做）
- 为"JS 彻底加载失败卡死 spinner"出了一套方案：两层（内联 classic `<script>` watchdog 覆盖脚本完全未执行 + `bootstrap()` try/catch 覆盖运行时错误），共用 `window.__bootError()` 替换 `#app` 为错误 UI；改动 index.html / main.ts / app-loading.css / 新增 d.ts。
- 用户决定暂不做：一般出错会去控制台看，或后续看情况再优化。已记 MEMORY.md 约定③标注"2026-07-22 已出方案，暂不做"。

## 沉淀工程化文档（已落地）
- 按用户要求，把已落地工程化条目整理为 `docs/前端工程化.md`：按 8 个分组（构建产物优化 / 类型与代码质量 / 开发体验 / 状态路由 / 请求层 / 样式UI / 首屏体验 / Skill），每条配一句话好处说明，列表形式、简单明了。
- 同步把 MEMORY.md「后续计划」里"整理工程化配置文档"由待启动改为已落地（指向 docs/前端工程化.md）。
- 文档规范修正：用户要求 `docs/前端工程化.md` 标题不贴「（已落地）」标识，且文档只列已实现的工程化条目（不混入未做项）。已移除标题里的「（已落地）」并改写引导语。
- oxlint 评估与文档补遗：用户问 oxlint 用法与好处，经核对——本项目已严格 TS + vue-tsc，未用变量/类型错误已被覆盖，console/debugger 构建期 Rolldown 已删；oxlint 相对增量有限（仅 eqeqeq/no-console/no-debugger 等规范类 + type-aware 反模式）。用户判断"没啥用"，决定暂不接入。已在 `docs/前端工程化.md` 末尾补「九、未做 / 待评估」小节，首条即 linter 评估结论（oxlint 非必需，暂不入），并列出其余已知未做项，使文档自洽。

## 构建期 gzip 预压缩（已落地）
- 用户最初贴 `vite-plugin-compression` v0.5 配置要求出方案，确认"只加 gzip"；落地时默认导入报"类型 typeof import 没有调用签名"——根因是该包是 CJS，在 `verbatimModuleSyntax`+`nodenext` 下默认导入无调用签名。用户指示换用 `vite-plugin-compression2`。
- 重写 `build/plugins/dist-compression.ts`：`compression` 默认导入 + `algorithms:[['gzip',{level:9}]]`、`threshold:10*1024`、`deleteOriginalAssets:false`、`include` 正则（js/css/json/html/ico/svg）、`logLevel:'silent'`（替代旧版 `verbose`/`ext`/`deleteOriginFile`/`filter`）。
- `build/plugins/index.ts` 导入并 `plugins.push(setupCompressionPlugin())` 注册。
- 验证：`node node_modules/vue-tsc/bin/vue-tsc.js -b` 类型零报错；`node node_modules/vite/bin/vite.js build` 构建成功，产物 `dist/js/vue-BtP63QpT.js.gz`(23.6KB vs 原60.7KB)、`dist/js/vue-router-CTM-RetJ.js.gz`(8.9KB vs 22.7KB)，压缩率约61%，<10KB 文件不压符合预期。
- 关键约束：`.gz` 只有服务端开启 `gzip_static`（nginx）或 CDN 预压缩才会被直接发出，否则不生效——部署侧需配套。
- MEMORY.md 已加「构建期 gzip 预压缩」约定条目 + 工程化进度「已完成」补 gzip（未做项收窄为 brotli/CDN/路由级懒加载）。
- 用户要求总结 gzip 好处并更新 `docs/前端工程化.md`：在「一、构建与产物优化」新增 gzip 子弹点（含压缩率实测与生效前提）；将「九、未做」里的 "gzip/brotli 压缩" 收窄为 "brotli 压缩"，使文档与实现自洽。
- 给 `build/plugins/dist-compression.ts` 的 `compression({...})` 各配置项补通俗易懂的 `//` 注释（algorithms 算法与等级 / threshold 体积门槛 / deleteOriginalAssets 保留原文件 / include 后缀范围 / logLevel 静默），仅注释改动、逻辑不变。
