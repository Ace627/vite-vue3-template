# 2026-07-23 工作日志

## 理解并沉淀「模拟登入登出 + 路由守卫」流程到项目记忆
- 探查确认本轮改动（纯模拟，非真实接口）：
  - 新增 `src/router/modules/static.route.ts`：`STATIC_ROUTE_LIST` 常量路由（dashboard/login/404），无 asyncRoutes/addRoute/roles。
  - 新增 `src/router/router.guard.ts`：`globalRouterBeforeGuard(to)`，白名单 `['/login']`、未登录 `redirect` 带原路径、仅依据 `getAccessToken()` 有无判断。
  - 改 `src/router/index.ts`：`routes` 改用 `STATIC_ROUTE_LIST`；`setupRouter` 注册守卫→`app.use`→`await isReady` 再挂载。
  - 新增 `src/views/core/login.vue`：预填 `admin`/`admin123456`（不校验），`handleLogin` 写死 `setAccessToken('mock-access-token')` + `replace` 回跳 `query.redirect`。
  - 改 `src/views/dashboard/index.vue`：新增「退出登录」+ `handleLogout` = `removeAccessToken()` + `router.replace('/login')`。
  - 未改动：`token.cache.ts`、`src/utils/request/**`（jwt-auth 带 Bearer、response-error 401 兜底已存在）。
- 记忆更新落点：`MEMORY.md` ①「路由」约定段拆三层并改述；②新增「登录/登出/守卫流程（模拟实现）」章节（主链路 + 模拟边界 + 升级真实登录最小路径）；③「工程化进度」已完成补「模拟登录·登出·路由守卫」，未做补真实登录接口/user store/动态权限路由/刷新 token。
- 登录态本质：localStorage 的 `accessToken` 字符串，无 user/auth Pinia store、无 userInfo/roles。`refreshToken` 系列为死代码。
- 本次仅更新记忆，未改动任何业务代码、未跑构建。

## 分析并记忆「路由 base 与 Vite base 对齐」改动（当前未提交）
- `git status`：`docs/前端工程化.md` 已暂存(staged)、`src/router/index.ts` 未暂存(unstaged)；`static.route.ts` 仅 CRLF→LF 换行符警告（非逻辑改动）。
- `src/router/index.ts` 改动：解构 `const { VITE_ROUTER_MODE, VITE_PUBLIC_PATH } = import.meta.env`，并把 `VITE_PUBLIC_PATH` 作为 base 传入 `createWebHashHistory(VITE_PUBLIC_PATH)` / `createWebHistory(VITE_PUBLIC_PATH)`。
  - 修复点：此前 Vite `base` 已支持 `VITE_PUBLIC_PATH`(commit e832611) 控制子路径部署，但 vue-router 的 history 未传 base（默认 `/`），`VITE_PUBLIC_PATH!=='/'` 时两者脱节，history 模式路由不匹配。
  - 自洽性：`VITE_PUBLIC_PATH` 已在 `.env:8`(`/`)、`vite.config.ts:13`(`base: runtimeConfig.VITE_PUBLIC_PATH ?? '/'`)、`vite-env.d.ts:9` 类型声明中存在，属补齐遗漏，非凭空引入。
- `docs/前端工程化.md` 改动（staged）：路由小节重写「三层结构+登录态守卫」、新增「登录/登出（模拟实现）」小节、未做清单补真实登录接口/user store/动态权限路由。属工程化文档沉淀，与 82807a3 一致。
- 提交提示：两处分离，若成对提交需把 `src/router/index.ts` 也 `git add`；本次无 `.workbuddy/` 改动。
- 本次仅分析+记忆，未改动业务代码、未提交、未跑构建。

## 评估并放弃「依赖 CDN external 插件」方案
- 讨论脉络：用户想替代停维护的 `vite-plugin-cdn`，把 npm 依赖（vue/pinia 等）构建时 external 化、改用 CDN importmap+ESM 引入；曾进 Plan 模式写计划（纯手动 deps 配置、每依赖独立外链、无默认值）。
- 用户最终决策：撤了，不搞，没啥需求。
- 已回退：计划批准后曾落地尝试（新建 `build/plugins/cdn-external.ts`、`index.ts` 注册、`vite.config.ts` 移除 vendor groups），但用户放弃，全部改动已撤销。当前 `build/plugins/` 仅原始 4 文件、`vite.config.ts` 的 `codeSplitting.groups` 恢复完整 5 个（vue/pinia/vue-router/axios/dayjs）。
- 结论：本项目当前不引入依赖 CDN external 化，保持「vendor 本地打包 + hash + immutable 缓存」方案。若未来再提，直接引用本记录，不必重开方案。

## 明确两项工程化决策（brotli / refreshToken）
- brotli 压缩：**明确不引入**，gzip 已足够；从「未做/待评估」移除，不再列为待办。
- refreshToken 系列（`setRefreshToken`/`getRefreshToken`/`removeRefreshToken`）：重新定性为**预留扩展点**（有意为之、当前未启用、未来接入 token 刷新机制时启用），非死代码、非未做项。
- 落地：①`MEMORY.md`「登录/登出/守卫流程」章节 refreshToken 措辞改为预留扩展点；②「工程化进度」未做清单移除「刷新 token」、`brotli·CDN·...` 改为 `CDN·路由级业务分包` 并加注 brotli 不引入；③`docs/前端工程化.md` 构建优化条注明「brotli 明确不引入，gzip 已足够」。
- 本次仅更新记忆+文档，未改业务代码、未跑构建。

## 落地「ElementPlus 单一来源精简 + 记忆/文档更新」
- 用户确认执行（前一轮已分析）：①`MEMORY.md` 的 EP 状态由「不引入」改为「已按需引入 + sass 主题 + 暗黑 css-vars 待切换」；②移除 `unplugin-element-plus` 做单一来源；③补进 `docs/前端工程化.md`。
- 源码改动：`build/plugins/index.ts` 删 `import ElementPlus from 'unplugin-element-plus/vite'` 与 `plugins.push(ElementPlus({ useSource: true }))`；`package.json` devDependencies 删 `unplugin-element-plus@^0.11.2`；`pnpm-lock.yaml` 手动删 3 处引用（importer/devDeps、packages spec、snapshots），保持 lock 与 pkg 一致（本机 pnpm corepack 垫片损坏，无法 `pnpm install` 自动重算）。
- 验证：`node node_modules/vite/bin/vite.js build --outDir dist-verify-tmp` → `vue-tsc -b` 绿 + 1618 模块 transform 通过、主 CSS 13KB、主色主题生效。**证明移除 `unplugin-element-plus` 后，仅靠 `ElementPlusResolver({ importStyle: 'sass' })` 仍完整支撑按需导入 + sass 换肤，冗余插件确可安全删**。
- 记忆落点：`MEMORY.md` ①技术栈补 `element-plus ^2.14.3`；②「约定」新增 ElementPlus 段（按需+resolver+additionalData 换肤+暗黑 css-vars 待切换+已移除冗余插件+暗黑主色未对齐提醒）；③样式分层段改「全局入口(当前空)」→引入暗黑 css-vars+body 样式；④「工程化进度」已完成补 ElementPlus 按需引入、未做把「UI组件库不引入」改为「主题切换(暗黑css-vars已就绪待接开关并对齐主色)」；⑤构建状态更新为 1618 模块 + pnpm/沙箱环境注记。
- 文档落点：`docs/前端工程化.md` 第六节「样式与 UI」新增 ElementPlus 按需+sass主题+暗黑css-vars 待切换小节；第九节「未做」移除「UI组件库(Element Plus 明确不引入)」、改为「主题切换(亮↔暗…)」。
- 注：`packages` spec 块里 `unplugin-element-plus@0.11.2` 的签名 `sha512-jr88e…` 已从 lock 移除；其原依赖 @nuxt/kit/es-module-lexer 等仍被 auto-import/components 引用，snapshot 保留无误。

## 纠错：unplugin-element-plus 非冗余，已恢复 + 补 tip-modal.ts 分析
- 用户提供新文件 `src/utils/tip-modal.ts`：显式 `import { ElLoading, ElMessage, ElMessageBox, ElNotification } from 'element-plus'`，封装 `TipModal.msgXxx/alertXxx/notifyXxx/confirm/prompt/showLoading` 等静态方法；经 `src/utils/index.ts` re-export，`login.vue` 调用 `TipModal.msgSuccess('登录成功')` → 已进打包图。
- 用户实测：删掉 `unplugin-element-plus` 后 `ElMessage` 样式丢失。→ **推翻上一条"冗余可删"结论，属我误判**。
- 根因（已用构建实证）：`ElementPlusResolver` 仅覆盖**模板组件** `<el-xxx>` 的样式；`ElMessage` 等**函数式 API** 从 `'element-plus'` 显式 import，其样式 resolver 不注入，必须靠 `unplugin-element-plus({ useSource: true })`。两插件互补，缺一不可。
- 验证（用 `node node_modules/vite/bin/vite.js build --outDir 临时目录` 绕过沙箱安全删除）：
  - 无插件：`grep '\.el-message' dist-probe-nop/assets/*.css` → **0 命中**（复现用户现象）。
  - 恢复插件：`grep -ro '\.el-message' dist-probe-yes/assets/*.css | wc -l` → **98 命中**；`vue-tsc -b` 绿。铁证成立。
- 已恢复：①`build/plugins/index.ts` 重新 `import ElementPlus` + `plugins.push(ElementPlus({ useSource: true }))`；②`package.json` 加回 devDep；③`pnpm-lock.yaml` 补回 3 处（importer/devDeps、`packages` spec、`snapshots`，用早先删除时记下的精确字符串/integrity 还原）。因本机 pnpm corepack 损坏无法 `pnpm install` 重算，手工还原保证 lock 与 pkg 一致。
- 记忆/文档纠错：`MEMORY.md` ①「约定」ElementPlus 段改为"两插件互补、缺一不可"，注明函数式 API 样式必须靠 `unplugin-element-plus`、曾误删已恢复；②「工程化进度」已完成去掉"已移除冗余"措辞、改"必需保留"；③`docs/前端工程化.md` 第六节 ElementPlus 小节同步纠正（互补+必需）。
- 教训沉淀（写进记忆）：移除依赖/插件前，必须先用真实触发路径构建验证（本次盲区是只用了 `<el-config-provider>`，没触发 `ElMessage` 这类函数式 API）。

## 新建 AI 代码约束文档 AGENTS.md
- 用户要求：AI 生成的代码约束——图标统一使用 SvgIcon；Element Plus 组件尽量用插槽塞 SvgIcon。
- 仓库此前无 AI 约束文件（grep/常见名 glob 均无 AGENTS.md/CLAUDE.md/.cursorrules），故**新建** `D:\Project\vite-vue3-template\AGENTS.md`（AI 编码工具默认自动读取，约束才能真正被"生成代码"时读到）。
- 内容：①图标统一 `<SvgIcon>`（禁 EP 图标/裸 svg/iconfont/第三方库；name=文件名去.svg；`color` 默认 currentColor、`size` 默认 1em；新增图标走 `pnpm clean:svg`）；②EP 组件图标位（el-button 内容区/el-icon 插槽/el-input prefix·suffix/自定义列等）优先 `<SvgIcon>` 放插槽，附 search/user 示例。
- 配套：`MEMORY.md`「约定」段补指针（AGENTS.md + 两条核心）。
- 注：AGENTS.md 是标准 agent 指令文件名，WorkBuddy/Claude/Cursor 等会自动加载；若用户本意是放进 `docs/` 或别的文件名，可再迁移。

## 追加 AGENTS.md 第三条：复用 src/utils 并统一导出
- 用户追加约束：utils 下有的方法直接复用；`index.ts` 有统一导出；没有的就提醒用户添加对应方法并导出。
- 先读 `src/utils/index.ts` 确认结构：当前 `export * from './validate' / './storage-cache.util' / './cache/token.cache' / './tip-modal'` 四个模块，`@/utils` 为唯一 barrel 入口（login.vue 的 `import { setAccessToken, TipModal } from '@/utils'` 印证）。
- AGENTS.md 追加「## 3. 复用 src/utils 已有方法，缺失则提醒用户补充并导出」：①先查 utils 已有则复用、禁重复造轮/内联重写；②统一从 `@/utils` 导入、禁子路径散装 import、禁自建导出层；③不存在时**不擅自新建**、改为提醒用户补方法并在 `index.ts` 导出（确需落地则加文件+补导出并明确告知）。
- `MEMORY.md`「约定」指针由"两条"改为"三条"并复述第三条。

## 重写 README.md（定位为"前端工程化实践/总结模板"）
- 用户要求：README 大概意思就是本项目只用来实践前端工程化并做总结。
- 原 README 偏"功能清单"（SKILL 说明 + 已落地约定逐条罗列），与"实践+总结"基调不符。
- 重写：①开头点明"用于实践前端工程化并做总结的 Vue3+TS+Vite 模板"，强调不是业务应用、是工程化练习场/总结样本；②加"适合/不适合做什么"；③快速开始（含 pnpm 不可用时的本地二进制兜底注记）；④把细节收口到 `docs/前端工程化.md` 与 `AGENTS.md` 两个链接（延伸阅读表），不再堆配置；⑤保留项目级 Skill 一句话指引。
- 未动 MEMORY.md 约定（README 内容非长期约定，仅日志记录）。

## 适配 README 为"可展示的工程化实践样本"（暗合能力佐证用途，不提简历）
- 用户补充意图：本项目会用作前端工程化实践的佐证物料，要求 README 适配成"可展示成果/样本"，但**通篇不出现"简历"二字**。
- 落地（`README.md` 重写）：①tagline 改为"一份**可展示的前端工程化实践样本**"；②引言强化"逐项落地、可运行、可解释"+"可验证样本：配置真实可用、决策有依据、收益可量化，适合用于技术分享、团队复盘或能力佐证"；③新增「这份样本展示了什么」能力域↔落地手段对照表（8 行，覆盖构建/类型/开发体验/状态路由/请求层/样式UI/首屏/Skill），让成果一眼可扫、便于讲解；④"适合做什么"把"学习/复盘"改为"**前端工程化能力展示 / 复盘**的样本：…可直接讲解"。
- 配套（`docs/前端工程化.md` 开头引导语）：在顶部 blockquote 补一句"本文是 README 中'前端工程化实践样本'的**配置详解**：样本展示'配了什么、为什么、收益几何'，这里逐条展开"，串成"样本 ↔ 详解"关系。

## 新建项目级 Skill：iterate-readme
- 用户要求：生成一个 Skill，作用是按"可展示的前端工程化实践样本"这一定位持续迭代 README.md。
- 落点：`D:\Project\vite-vue3-template\.workbuddy\skills\iterate-readme\SKILL.md`（项目级；用户未提全局/用户级，按记忆规则写当前工作空间）。
- Skill 内容：固化核心主旨（一句话定位 + 三锚点 + "简历"字样硬约束=0 命中）、结构骨架（tagline→引言三锚点→能力域对照表→适合/不适合→快速开始→延伸阅读→项目级 Skill 指引）、能力域清单（8 行，与 docs 八章节一一对应）、执行步骤（读现状→grep 简历→对齐能力表与 docs→对照主旨→**先出方案等确认**→落地→二次 grep 核验→小结）。
- 设计贴合用户习惯：执行前先出具体改动清单、不自动 git commit、不动无关文件；并与 `docs/前端工程化.md`（配置详解）保持配套呼应。
- 注：计划模式本会话 `ExitPlanMode` 工具异常（参数无法解析为对象），改以 AskUserQuestion 取得用户"确认创建"授权后落地。README「项目级 Skill」小节与 docs「工程化 Skill」节仍列 3 个，未同步补第 4 个（未主动改无关文件，待用户确认是否补）。
- 全程规避"简历"字样；`AGENTS.md` 不动。
