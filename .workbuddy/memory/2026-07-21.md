# 2026-07-21 工作日志

## 集成 Pinia 状态管理
- `pnpm i pinia` 安装 `pinia@4.0.2`（运行时依赖，进 `dependencies`，非 -D）。
- 新建 `src/store/index.ts`：模块级单例 `const store = createPinia()` + `export function setupStore(app: App) { app.use(store) }`（用户给定代码，原样落地）。
- 改 `src/main.ts`：`bootstrap()` 内于 `createApp(App)` 之后、`app.mount('#app')` 之前插入 `setupStore(app)`。
- 验证：`pnpm build` 通过（`vue-tsc -b` 无报错，22 模块，JS 59.16→62.50kB，dist 正常）。

## 修正 main.ts 风格（用户截图反馈）
- `src/main.ts` 的 store 导入从 `@/store` 改回 `./store`（入口文件按同目录相对路径风格）。
- `// 配置 Store 状态管理 https://pinia.web3doc.top` 从 import 行尾移到 `setupStore(app)` 调用上方作为独立注释行。
- 验证：`pnpm build` 仍通过。
- 待确认：项目全局约定「导入统一用 `@/` 而非相对路径」与 `main.ts` 当前相对路径风格（`./App.vue`、`./styles/index.scss`、`./store`）存在冲突，需用户决定是否调整全局约定或统一 main.ts 导入方式。

## 统一 main.ts 导入为 @/（用户最终确认）
- 用户手动将 `main.ts` 全部改为 `@/` 风格：`@/styles/index.scss`、`@/App.vue`、`@/store`；`virtual:uno.css` 与 `@/styles/index.scss` 顺序微调（uno.css 在前）。
- 验证：`pnpm build` 通过（22 模块，无类型报错）。
- 结论：项目全局约定「导入统一用 `@/`」对入口文件 `main.ts` 同样生效，不再使用相对路径，上述冲突已消除。

## 接入环境变量与标题注入
- 根目录新建 `.env`：`VITE_APP_TITLE="Vite Vue3 Template"`（公开构建期变量，注释「应用标题」）。
- `index.html` 的 `<title>` 由 `vite-vue3-template` 改为 `%VITE_APP_TITLE%`，由 Vite 原生 HTML env 替换注入（dev/build 均生效）。
- 验证：`pnpm build` 通过，`dist/index.html` 的 title 正确变为 `<title>Vite Vue3 Template</title>`（dotenv 自动去除外层双引号）。
- 注意：当前 `.gitignore` 未忽略 `.env`，会被提交；若后续加入密钥需补忽略规则。

## 自研 HTML meta 注入插件（name 借用 vite-plugin-html）
- 目标：让 meta 注入同时支持 env、package.json、后续 buildTime 等（用户原想用 `vite-plugin-html` npm 包，但本项目 Vite 8 + 该包 `peer: vite>=2.0.0` 旧包，运行时兼容性未验证；最终决定手搓插件，仅复用其名）。
- `vite.config.ts`：改为函数式 `defineConfig(({ mode }) => …)`；用 `loadEnv(mode, process.cwd())` 读 `.env`；用 `import pkg from './package.json' with { type: 'json' }` 替代原 `readFileSync`+`JSON.parse`（更简洁）。插件 `name: 'vite-plugin-html'`，`transformIndexHtml` 以 map 替换占位符：`__APP_DESCRIPTION__` = `env.VITE_APP_DESCRIPTION` 覆盖优先、回退 `pkg.description`；`__BUILD_TIME__` = `new Date().toISOString()`（预留扩展位）。
- `tsconfig.node.json`：新增 `resolveJsonModule: true`（否则 `vue-tsc -b` 因 JSON 导入报错；且 `module: nodenext` 下 JSON 导入必须带 `with { type: 'json' }` 属性）。
- 验证：`pnpm build` 通过（vue-tsc 无报错，22 模块）；`dist/index.html` 实测 description 被正确注入为 package.json 文案、title 为原生 `%VITE_APP_TITLE%` 替换值。
- 遗留/环境坑：`index.html` 写入被外部进程回滚——Edit 工具报「成功」但实际未落盘（与早前 `vite.config.ts` 的 EBUSY 同源，疑为文件锁/同步进程）；Python 直接写也被回滚。**`__BUILD_TIME__` 的 meta 标签（`<meta name="build-time" content="__BUILD_TIME__" />`）因此暂未写入 index.html**，插件能力已就绪，待锁解除后由用户手动补或重试。

## 占位符改为 Vite 原生 %xxx% 格式
- 用户要求：`env.VITE_APP_DESCRIPTION` 不会存在（`.env` 不配它），占位符改成 Vite 原生 `%xxx%` 风格更统一。
- `vite.config.ts`：移除 `loadEnv` 与 env 覆盖逻辑，回到对象式 `defineConfig({...})`；`transformIndexHtml` 占位符改为 `%VITE_APP_DESCRIPTION%`（= `pkg.description`）、`%BUILD_TIME%`（= `new Date().toISOString()`）。
- `index.html` 目标：`<meta name="description" content="%VITE_APP_DESCRIPTION%" />` + `<meta name="build-time" content="%BUILD_TIME%" />`；但**写入被同一文件锁回滚**，磁盘仍是旧 `__APP_DESCRIPTION__`、无 build-time 标签。
- 为不破坏构建：插件**同时兼容新旧占位符**（`%VITE_APP_DESCRIPTION%`/`__APP_DESCRIPTION__`、`%BUILD_TIME%`/`__BUILD_TIME__`）。验证：`pnpm build` 通过，dist 的 description 仍正确注入为 package.json 文案、title 为原生 `%VITE_APP_TITLE%` 替换值。
- 后续：锁解除后把 `index.html` 改成 `%xxx%` 并删插件里的 `__xxx__` 兼容分支。

## 撤掉双兼容分支（用户反馈会无限膨胀）
- 用户指出「新旧占位符同时兼容」会随占位符增多而无限膨胀，不可取。已撤掉：插件回到**单格式** `transformIndexHtml(html) { return html.replace('__APP_DESCRIPTION__', pkg.description ?? '') }`，只匹配锁死的 `index.html` 当前占位符 `__APP_DESCRIPTION__`，`pnpm build` 仍绿。
- 策略修正：插件格式必须与 `index.html` 实际占位符一致，**不保留旧分支**；待 `index.html` 可编辑（锁解除后由用户在 IDE 手动改，或锁消失后我直写）改成 `%VITE_APP_DESCRIPTION%` + 加 `<meta name="build-time" content="%BUILD_TIME%">`

## index.html 已由用户改 %xxx% 并翻插件（终态）
- 用户在 IDE 将 `index.html` 改为 `%VITE_APP_DESCRIPTION%` + `<meta name="build-time" content="%VITE_APP_BUILD_TIME%">`（build-time 占位符用户命名为 `%VITE_APP_BUILD_TIME%`，与前缀统一）。
- `vite.config.ts` 插件翻为单格式 `%VITE_APP_*%`：`%VITE_APP_DESCRIPTION%`=pkg.description、`%VITE_APP_BUILD_TIME%`=构建时刻 ISO。无遗留旧分支。
- 验证：`pnpm build` 通过，dist 实测 description=package.json 文案、build-time=时间戳、title=原生 `.env` 值。三处 meta/title 全用 `%VITE_APP_*%` 原生风格，完成。

## 插件落地「裸名 map + 自动 % 包裹 + 正则自动生成」（用户授权「先试试看」）
- `vite.config.ts` 插件最终实现：维护 `data: Record<string, string> = { VITE_APP_DESCRIPTION: pkg.description ?? '', VITE_APP_BUILD_TIME: new Date().toISOString() }`；`pattern` 由 `Object.keys(data).map(k => \`%${k}%\`.replace(/[.*+?^${}()|[\]\\]/g, '\\$&')).join('|')` 自动生成（含正则特殊字符转义），替换回调 `html.replace(pattern, (m) => data[m.slice(1, -1)] ?? m)` 剥回裸名查 map。新增占位符只需在 `data` 写一行裸名，无需手写 `%` 也无需维护正则。
- 验证：`pnpm build` 通过（3.24s）。`dist/index.html` 抽查三处均正确：`description` = `本项目提供了一个基于 Vue3、TypeScript 和 Vite 的基础配置模板...`（package.json）、`build-time` = `2026-07-21T01:30:37.372Z`（时间戳）、`title` = `Vite Vue3 Template`（`.env` 原生替换）。方案验证通过，无遗留旧分支、无手写正则。

## 对比研究 vbenjs/vite-plugin-html 官方实现
- 用户想看官方实现。结论：它是 pnpm monorepo（`packages/core`），`createHtmlPlugin()` 组合 `createPlugin(htmlPlugin.ts)` + `createMinifyHtmlPlugin(minifyHtml.ts)` 两个插件。
- 占位符机制用 **EJS 模板引擎**（`import { render } from 'ejs'`）：`renderHtml` 把 `viteConfig.env` + `viteConfig.define` + `loadEnv()` + `inject.data` 合并成 `ejsData`，再 `render(html, ejsData, ejsOptions)`。HTML 里写 EJS 语法 `<%- title %>`、**不是** Vite 原生 `%VAR%`、也不是正则。
- 钩子：`name: 'vite:html'`、`enforce: 'pre'`，`transformIndexHtml`（Vite>=5 用 `{order:'pre', handler}`）；另含 `configResolved`(loadEnv)、`config`(MPA 的 rollup input)、`configureServer`(history fallback)、`closeBundle`(搬 html 到 outDir 根)。
- 能力：EJS 模板、多页 MPA、自定义 entry（注入 module script 并删原脚本）、tags 注入、HTML minify(html-minifier-terser)、SPA history。
- 依赖：`ejs`、`node-html-parser`、`fs-extra`、`pathe`、`fast-glob`、`consola`、`colorette`、`connect-history-api-fallback`。
- 结论：我们「不装 vben、手搓借名」的取舍合理——vben 是整套 HTML 工程化方案（且用 EJS 需改 HTML 语法、引入 ejs 等依赖），我们只需 meta 注入，手搓方案用 Vite 8 原生 `%VITE_*%` + 极简 map（~15 行、0 依赖），更轻且天然契合「单一真源 + 原生风格」约定。

## 彻底移除手搓插件（用户决定回退，仅留 Vite 8 原生 title）
- 用户最终决定撤销手搓插件，回到「仅 Vite 8 原生 `<title>%VITE_APP_TITLE%</title>`」。`index.html` 的 description / build-time 两个 meta 彻底移除。
- `vite.config.ts`：删 `import pkg from './package.json' with { type: 'json' }`，删 `plugins` 里的 `{ name: 'vite-plugin-html', transformIndexHtml(...) }` 整段，现 `plugins: [vue(), UnoCSS()]`。
- `index.html`：删 `<meta name="description" content="%VITE_APP_DESCRIPTION%">` 与 `<meta name="build-time" content="%VITE_APP_BUILD_TIME%">` 两行，仅留 title（已核实磁盘真实落盘，无回滚）。
- `tsconfig.node.json`：撤 `resolveJsonModule: true`（仅为那个 JSON import 加的）。
- 验证：`pnpm build` 通过（3.43s），`dist/index.html` 的 `<title>Vite Vue3 Template</title>` 正确、`grep -c` 确认无 `%VITE_APP_DESCRIPTION%`/`%VITE_APP_BUILD_TIME%`/`%VITE_APP_TITLE%` 残留。
- `.env` 的 `VITE_APP_TITLE` 保留（title 仍走原生替换）；`package.json` 的 description 仅是 npm 描述，不再注入 HTML。
- 备注：后续若需 meta 注入，优先走 Vite 原生（`.env` 加 `VITE_*` 变量即可被 `%VAR%` 命中）；纯动态值（如构建时间戳）Vite 原生无法提供，要么放弃、要么重引插件/构建脚本。

## tsconfig.node.json 补 JSDoc 注释（对齐 tsconfig.app.json 风格）
- 用户要求 `tsconfig.node.json` 也加上类似 `tsconfig.app.json` 的逐选项注释。已用 JSDoc `/** ... */` + `@description`/`@note` 块注释风格为全部 compilerOptions 与 include 补说明（tsBuildInfoFile/target/lib/types/skipLibCheck/module/allowImportingTsExtensions/verbatimModuleSyntax/moduleDetection/noEmit/noUnusedLocals/noUnusedParameters/erasableSyntaxOnly/noFallthroughCasesInSwitch/include），并加文件级「Node 端 TS 配置」说明块（与 tsconfig.app.json 的 extends 说明块对应）。
- 验证：`pnpm build` 通过（2.95s），注释不影响 `vue-tsc -b` 与打包。

## Vite 插件抽离到 build/plugins/index.ts（对标 vite-electron-template）
- 参考 `Ace627/vite-electron-template` 的 `build/plugins/index.ts`：导出 `setupVitePlugins(isBuild)` 函数，内部 `plugins.push(...)` 逐个注册插件，返回 `PluginOption[]`。
- 用户要求「完全对标参考，纳入类型检查」。落地：
  - 新建 `build/plugins/index.ts`：导出 `setupVitePlugins(): PluginOption[]`，`plugins.push(vue())` + `plugins.push(UnoCSS())`，中文 JSDoc 注释（结构完全对标）。
  - `vite.config.ts`：`plugins: setupVitePlugins()`，删掉内联的 `vue()`/`UnoCSS()` 与对应 import。
  - `tsconfig.node.json`：`include` 改为 `["vite.config.ts", "build/**/*.ts"]`，把 `build/` 纳入 `vue-tsc -b` 类型检查。
- 与参考的差异：① 省略 `isBuild` 参数（纯前端用不到，且 `noUnusedParameters` 会因未用参数报错）；② nodenext 下相对导入须带扩展名，故写 `./build/plugins/index.ts`（参考用 bundler 解析可省略，这是唯一写法差异）。
- 踩坑：`vue-tsc -b` 首次报 `vite.config.ts(3,34): error TS2834`（`./build/plugins` 缺扩展名），改为 `./build/plugins/index.ts` 后通过——证明 `build/` 确实被纳入类型检查。
- 验证：`pnpm build` 通过（3.42s），`dist/index.html` 的 title 正确、`dist/assets/*.css` 1040B（UnoCSS 正常生效）。

## 集成 unplugin-auto-import + unplugin-vue-components
- 参考 `Ace627/vite-electron-template` 的 `build/plugins/auto-import-plugin.ts`，但去 ElementPlus（用户要求不处理）、去 `vue-router`（项目未装路由）。
- 安装：`pnpm i unplugin-auto-import unplugin-vue-components -D`（用户给的命令），落 `unplugin-auto-import@21.0.0` + `unplugin-vue-components@32.1.0`。
- 新建 `build/plugins/auto-import-plugin.ts`：导出 `registerAutoImport()`（`AutoImport({ imports:['vue','pinia'], dts:'src/types/auto-generate/auto-import.d.ts', dirs:[] })`）与 `registerAutoComponents()`（`AutoComponents({ dts:'src/types/auto-generate/auto-components.d.ts', dirs:[] })`）。
- `build/plugins/index.ts`：`import { registerAutoImport, registerAutoComponents } from './auto-import-plugin.ts'`（nodenext 带 `.ts`），在 `setupVitePlugins()` 内 `plugins.push(registerAutoImport())` + `plugins.push(registerAutoComponents())`。
- `.gitignore`：追加 `src/types/auto-generate/`（d.ts 为生成产物）。
- 全局组件目录：用户明确 `src/components` 不合适（类型/组件名后续自定义），故 Components 的 `dirs` 暂留 `[]`，等确定目录（如 `src/global-components`）再配。
- 验证：`pnpm build` 通过（1.89s）；`vue-tsc -b` 无报错（证明 `build/plugins/auto-import-plugin.ts` 被纳入类型检查）；`[unplugin-vue-components] no components found` 是 `dirs:[]` 的预期提示非错误；生成 `src/types/auto-generate/auto-import.d.ts`(4323B)，`auto-components.d.ts` 因无组件暂未生成（正常）；dist 体积不变。

## registerAutoImport 的 dirs 恢复为 src/store/modules + src/hooks
- 用户要求把 `registerAutoImport` 的 `dirs` 从 `[]` 改回参考原值 `['src/store/modules', 'src/hooks']`，并真实建目录。
- 新建 `src/store/modules/.gitkeep` 与 `src/hooks/.gitkeep`（空目录占位，便于 Git 跟踪）。
- `build/plugins/auto-import-plugin.ts` 的 `registerAutoImport` 改 `dirs: ['src/store/modules', 'src/hooks']`；与 `main.ts` 显式导入的 `@/store`(`src/store/index.ts`) 不冲突——`dirs` 仅扫 `modules/` 子目录，不扫 `index.ts`。
- 验证：`pnpm build` 通过（3.52s），目录确认存在（各含 `.gitkeep`）。`registerAutoComponents` 的 `dirs` 仍 `[]`（全局组件目录约定待定）。

## 删除 .gitkeep 占位文件
- 用户明确「不需要 .gitkeep 文件」。删除 `src/store/modules/.gitkeep` 与 `src/hooks/.gitkeep`，目录保留为空（空目录默认不被 Git 跟踪，用户接受）。
- 同步修正 MEMORY.md 的 auto-import 约定描述（不再写「各含 .gitkeep」）。

## 提交（git-commit-msg skill 流程）
- 按项目 `git-commit-msg` skill：生成 5 条候选中文 message → 用户选 #2。
- 用户确认 `.workbuddy/`（含 `memory/`）一并入库，与历史惯例一致（仓库本就跟踪 `.workbuddy/skills/` 与 `memory/`）。`.env` 保留提交。仅提交暂存区、不推送。
- 提交信息：`chore: 抽离 Vite 插件至 build/plugins 并集成 unplugin 自动导入`。
- 含本次全部暂存改动：`.env`、`.gitignore`、`build/plugins/index.ts`、`build/plugins/auto-import-plugin.ts`、`index.html`、`package.json`、`pnpm-lock.yaml`、`src/main.ts`、`tsconfig.node.json`、`vite.config.ts`、`.workbuddy/memory/2026-07-21.md`、`.workbuddy/memory/MEMORY.md`。

## 进度盘点（用户要求"再分析下进度"）
- 全量核对配置与产物（package.json / vite.config.ts / build/plugins / tsconfig.* / src 结构 / .env / .gitignore / README / index.html），并实跑 `pnpm build` 验证。
- 结论：工程化基建层约 95% 完工——`vue-tsc -b` 类型检查 + `vite build` 均通过（22 模块，dist 正常），路径别名、Pinia、UnoCSS、auto-import/auto-components、bootstrap 引导、reset.css、.gitattributes 全部落地；`src/hooks` 与 `src/store/modules` 空目录已建。
- 应用业务层为 0%（App.vue 仅注释占位，模板本就不含业务；路由/请求层/全局组件目录均未引入）。
- 文档一致性约 60%：发现 README 仍称"经 Vite 插件自动注入 <meta name="description">"，但实际 meta 注入插件已于今日早些时候彻底移除，index.html 仅保留 `%VITE_APP_TITLE%` title；README 也未提及 Pinia / auto-import 等新能力，整体滞后于实现，待更新。

## 更新 README.md（对齐实现）
- 重写「已落地的工程化约定」章节：删除已过时的「经 Vite 插件自动注入 <meta name="description">」描述（实际 meta 注入插件今日已移除，index.html 仅保留 `%VITE_APP_TITLE%` 原生标题注入）；补充 Pinia、API 自动导入（unplugin-auto-import）、组件自动导入（unplugin-vue-components）、Vite 插件抽离到 build/plugins、bootstrap 引导、.gitignore 忽略 auto-generate 等真实约定。
- 文档一致性由 ~60% 提升至与实现对齐；README 现在准确反映脚手架现状。

## 理解并跟进 vue-router 手动集成（用户手动改动）
- 用户手动集成 vue-router：package.json 加 `vue-router ^5.2.0`；新建 `src/router/index.ts`（模块级单例 `router` + `setupRouter(app)`：app.use + await isReady；history 模式按 `import.meta.env.VITE_ROUTER_MODE==='hash'` 切 hash/webHistory）、`src/layout/index.vue`（布局组件，含 `<router-view>`、`defineOptions name:'Layout'`、scoped scss）；`App.vue` 改为 `<router-view>`；`main.ts` 的 `bootstrap()` 在 `setupStore` 后、`mount` 前 `await setupRouter(app)`；`.env` 加 `VITE_ROUTER_MODE="history"`。
- 集成符合既有约定：bootstrap 异步引导、`@/` 别名、注释风格、模块级单例写法对齐 Pinia。
- 待确认/半成品：`routes` 仅根路由 `path:'' component:Layout`、无 `children`、无业务页（Layout 内 `<router-view>` 暂无子路由可渲染）；`auto-import-plugin.ts` 的 `imports` 当时仍 `['vue','pinia']` 未含 vue-router（与"已装路由"矛盾，已后续处理）；`vue-router ^5.2.0` 版本存疑（Vue3 生态通常配套 4.x，5.x 需核实）。

## 处理 auto-import-plugin.ts 的 vue-router imports（用户要求"处理下"）
- `build/plugins/auto-import-plugin.ts` 的 `registerAutoImport`：`imports` 由 `['vue','pinia']` 改为 `['vue','pinia','vue-router']`，注释由"无 vue-router 故不引入"改为"项目已装 vue、pinia、vue-router，对应 API 自动导入"。
- 同步更新 README「API 自动导入」行：`imports: ['vue','pinia','vue-router']`。
- 验证：`pnpm build` 通过（vue-tsc -b 无报错，29 模块，JS 86.64kB 含 vue-router，dist 正常）；auto-import.d.ts 重新生成含 vue-router API。
- 顺带修正 MEMORY.md 过时表述（见 MEMORY.md 末尾「更新补记」）：原「刻意去掉 imports 里的 'vue-router'（本项目未装路由）」已过时，现已加回。

## 联网核实 vue-router 版本（用户要求"开眼看世界"）
- 结论：`vue-router ^5.2.0` 真实且正确。npm registry dist-tags：`latest=5.2.0`（2026-07-15 发布）、`next=4.0.13`、`legacy=3.6.5`、`beta=5.0.0-beta.2`。vue-router 已于 2026 年发布 5.x 大版本并切 latest，此前"Vue3 配套 4.x"系过时认知。
- 修正：上条 MEMORY.md 补记的"⚠️ 版本存疑"判断不成立，已追加「更正」段说明。
- 影响：无需改版本；现有路由代码 `pnpm build` 已通过与 5.2.0 兼容。

## 提交（git-commit-msg skill 流程）
- 按项目 `git-commit-msg` skill：生成 5 条候选中文 message → 用户选 #1。
- 用户明确 `.workbuddy/`（含 `memory/`）一并入库，理由「方便其它人使用 AI 快速理解项目」；新建 `pnpm-workspace.yaml`（pnpm 构建许可配置 `allowBuilds: { '@parcel/watcher': false }`）一并提交。
- 提交信息：`feat: 集成 vue-router 并新增 Layout 布局组件`（commit `8d0d284`）。
- 12 文件、295 行：`.env`、`README.md`、`build/plugins/auto-import-plugin.ts`、`package.json`、`pnpm-lock.yaml`、`pnpm-workspace.yaml`、`src/App.vue`、`src/layout/index.vue`、`src/main.ts`、`src/router/index.ts`、`.workbuddy/memory/2026-07-21.md`、`.workbuddy/memory/MEMORY.md`。
- 提交方式：仅本地提交、不推送（延续历史惯例）；含 `.workbuddy/` 入版本库。

## 更正（本次提交已撤销并重提）
- 上方「## 提交（git-commit-msg skill 流程）」记录的 commit `8d0d284` 已被用户撤销（git reset），现重新提交：message 仍用 #1 `feat: 集成 vue-router 并新增 Layout 布局组件`，含 `.workbuddy/`（含 memory/，理由：方便他人用 AI 快速理解项目）、仅本地提交不推送。
- 纪律修正：为避免"提交后又补记 daily log 产生脏记录"，本次改为**提交前**将 memory 补完、**提交后不再写** daily log，故本段不写新 commit hash。
