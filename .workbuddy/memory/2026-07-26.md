# 2026-07-26

## docs/前端工程化.md 同步更新
核对文档与代码（defaultSettings.ts / useTheme.ts / settings/index.vue / setting.ts / static.route.ts / styles/index.scss / layout/components/AppLogo/），发现文档滞后于 2026-07-24~25 落地实现。经用户确认，更新 `docs/前端工程化.md` 5 处：
1. 第六章样式分层后补「`.app-content` 业务路由页基础容器」（统一 padding、移动端收窄）
2. 第六章新增「品牌标识组件（AppLogo）」——用户明确其定位为**项目 Brand（logo+标题）的抽离封装**，路径 `src/layout/components/AppLogo/`（index.vue+types.ts），props `showTitle`/`title`（默认 VITE_APP_TITLE），logo 用 img 引用 favicon（品牌图，合规）；不依赖全局状态、可复用
3. 第六章「主题切换」段改写：`SystemSetting` 补 `size` 字段；新增 `useTheme()` 统一逻辑层（ThemeSwitch 与设置页共用）；「为后续设置面板预留数据层」改为「已由设置页接入」
4. 第六章新增「设置页 `/settings`」段（路由注册、Navbar 设置入口 SvgIcon、主题即时保存 / 尺寸·动态标题预览式落盘 / 保存·重置 / 移动端覆盖）
5. 第九章 Navbar 描述校准：标注 AppLogo 品牌组件、设置入口、设置页已落地

注意：中途一次 Edit 调用报内部错误，仅第1项先成功，后续补执行第2~5项；最终读回归校验，5 项全部正确落地。

## docs 补充：全局组件尺寸生效链路
读取 `src/App.vue` 确认 `<el-config-provider :size="settingStore.size">` 包裹根组件，把 store.size 注入 Element Plus 全局。在第六章「设置页 /settings」段后新增「全局组件尺寸生效链路」条：`SystemSetting.size` → `useSettingStore` → `App.vue` 的 `el-config-provider :size` → 所有 `<el-xxx>` 即时生效；落盘后刷新保持。同时注明同节点注入了 `element-plus` 的 `zh-cn` 语言包（仅 EP 组件文案中文化，非业务 i18n）。

## 工程化文档补充：全局水印
与用户确认「仅补文档、不改代码」后，在 `docs/前端工程化.md` 第六章「设置页 /settings」段后新增「全局水印（el-watermark）」条：layout 包裹全站、content=VITE_APP_TITLE、wh-full 铺满、gap、font 绑定 `useTheme().isDark` 做明暗联动、按需解析零成本、z-index:9 不遮挡弹窗。同步在 `MEMORY.md` 工程化进度登记该能力（2026-07-26）。代码保持现状（未加开关、`:font` 简写未改）。

## 布局梳理：AppMain 积木块（仅分析，未改代码）
用户新拆分 `src/layout/components/AppMain/index.vue`，把原本内联在 `layout/index.vue` 的 `<RouterView>` 抽出为独立布局积木块。要点：
- 职责单一：`<main class="app-main">` 包裹嵌套路由出口，负责「路由转场 + 内容渲染」，不含 Navbar / 水印（父级 layout 负责）。
- 转场：`Transition :name="settingStore.transition" mode="out-in"`，动效名来自 `SystemSetting.transition`（默认 `fade-transform`，自定义类在 `styles/transition.scss`；其余 `el-fade-*`/`el-zoom-*` 为 EP 内置）。
- `<component :key="route.path">`：按 path 强制重挂载（注释已说明 path vs fullPath 取舍）。
- **`KeepAlive :include="[]"` 当前为空数组 → 实际是 no-op 占位**，缓存能力未接（无 cachedViews 列表驱动）。
- 样式仅 `position:relative;width:100%;overflow-x:hidden`，无 `flex:1`/高度/纵向滚动 → 依赖内容自然撑开、整页在 body 级滚动（与 `.app-content` 逐页 padding 模型一致）。
- 合规：无图标/工具依赖，`useSettingStore` 经 dirs 自动导入，符合 AGENTS.md 约定。
- 关联：layout 组合顺序 `el-watermark(wh-full) > header(Navbar) + AppMain`；Navbar 用 `AppLogo`+设置入口(`SvgIcon name="Setting"`)+`ThemeSwitch`。
未做：KeepAlive 是否接 cachedViews、AppMain 是否需成为独立滚动容器（当前非 flex 子项）。待用户定。

## 文档补充：AppMain 落地到「前端工程化.md」
经用户确认「先补文档」，在 `docs/前端工程化.md` 补两处（仅文档、未改代码）：
1. 第六章「样式与 UI」末尾新增「布局组成（AppMain 内容区出口）」条：定位（从 layout 内联 RouterView 抽出的独立积木块、职责单一）、`<main>` 语义化、Transition 转场（settingStore.transition / fade-transform 自定义类在 transition.scss、其余 EP 内置）、`key=route.path` 强制重挂载、KeepAlive `:include="[]"` 为占位 no-op、样式仅 overflow-x 无独立滚动、合规（无图标/utils 依赖、store 自动导入）。
2. 第九章「未做/待评估」的 Layout 实际布局条改写：明确 AppMain 内容区已从内联抽离为 `src/layout/components/AppMain/index.vue`，Navbar/AppLogo/ThemeSwitch/设置页/AppMain 均落地，剩余侧边栏/菜单。

## 代码微调：AppMain 样式 overflow
`src/layout/components/AppMain/index.vue` 的 `.app-main` 由 `overflow-x: hidden` 改为 `overflow: hidden`（用户在 IDE 改完，已确认落地）。同步 `docs/前端工程化.md` 第六章描述 `overflow-x:hidden` → `overflow:hidden`，保持文档与代码一致。注：当前 `.app-main` 无固定高度（内容自然撑开），`overflow:hidden` 与 `overflow-x:hidden` 实际表现一致；若后续把它做成 `flex:1` 固定高度滚动容器，需注意 `overflow:hidden` 会裁切纵向内容，届时可能要换 `overflow-y:auto`。

## 新增：系统水印显示开关（showWatermark）
经与用户讨论最佳实践（运行时配置 vs 构建期 env；`<component :is="'template'">` 不可行——原生 `<template>` 会把子节点丢进 `.content` 导致整页消失），确认采用「最简 DRY」方案（content 置 `[]`，接受无害空层）。改动三处：
1. `src/defaultSettings.ts`：`SystemSetting` 新增 `showWatermark: boolean`，`defaultSettings` 默认 `true`。
2. `src/layout/index.vue`：取 `useSettingStore()`；`el-watermark` 的 `:content` 由 `VITE_APP_TITLE` 改为 `computed<string|string[]>(() => showWatermark ? VITE_APP_TITLE : [])`；关闭时 el-watermark 仍在但透明空层（无害）。
3. `src/views/settings/index.vue`：「动态标题」下新增「系统水印」`el-switch v-model="settingStore.showWatermark"`（预览式，经「保存设置」落盘，与 showDynamicTitle 一致）。store 无需改（`toRefs` 自动暴露）。
`vue-tsc -b` 通过（exit 0）。同步更新 `MEMORY.md` 约定（store 备注、SystemSetting 定义、设置页描述）。`docs/前端工程化.md` 尚未同步此开关，待定。
**设计决策（用户明确）**：不引入构建期 `VITE_SHOW_WATERMARK` 总开关——本项目定位是纯前端工程化实践模板（供实践与别人参考），无需生产锁死水印；运行时 `showWatermark` 开关即终态，勿再当待办项追加。

## 新增：路由转场配置项（settings 页）
`SystemSetting.transition` 早已存在（默认 `fade-transform`），由 `AppMain` 的 `<Transition :name="settingStore.transition">` 驱动，但此前设置页未暴露。本次在 `src/views/settings/index.vue` 的「系统水印」下新增「路由转场」行：`<el-select v-model="settingStore.transition">` 提供 6 个选项（fade-transform 自定义滑动位移 / el-fade-in-linear / el-fade-in / el-zoom-in-center / el-zoom-in-top / el-zoom-in-bottom），预览式、经「保存设置」落盘，切换后下一次路由跳转即生效。`el-select`/`el-option` 由组件解析器自动导入，无需手写 import。`vue-tsc -b` 通过（exit 0）。同步更新 `MEMORY.md` 设置页描述。

## 文档同步：README.md 与 docs/前端工程化.md 对齐代码现状
扫描全量源码逐项核对两份文档，修正与代码不同步处（仅改文档、未动代码）：
- **README.md（3 处）**：「样式与 UI」行补「设置页 / 全局水印 / 移动端适配」；能力域表「工程化沉淀」与「项目级 Skill」章节项目级 Skill 由 3 个改 4 个（新增 `iterate-readme`）并标注各自作用。
- **docs/前端工程化.md（7 处）**：①「主题切换」段 `SystemSetting` 字段补全为 `{theme, size, showDynamicTitle, showWatermark, transition}`；②「设置页」由「三项设置」改「五项设置」，修正「size 不落盘」错误（`saveSetting` 保存整个 `SystemSetting`，size 同样持久化）；③「全局水印」补 `showWatermark` 开关控制（content 绑 `computed(() => showWatermark ? VITE_APP_TITLE : [])`，关闭不渲染水印内容）——补上此前遗留的待定项；④「.app-content」补移动端断点来源（`useResize` 监听宽度 768px 写 `html[data-device]`）；⑤「oxfmt」删「sortImports / 启用 import 排序」错误描述（配置中已注释禁用）；⑥「工程化 Skill」由 3 改 4（加 `iterate-readme`）；⑦「未做」路由 children/懒加载改为「layout 下 dashboard/settings 已懒加载，未进一步按业务域拆包」。
- **核验**：grep 确认文档无「3 个项目 / 三项设置 / size 不落盘 / sortImports」残留，README 无「3 个项目级」残留。
