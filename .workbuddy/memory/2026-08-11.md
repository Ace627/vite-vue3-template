# 2026-08-11

## 从 jczz-data-analysis 迁移 UserDropDown 到本项目
- 目标：让 Navbar 内 `<!-- 个人中心 --> <UserDropDown class="navbar-item hover-effect" />` 生效（仅动相关文件）。
- 新增 `src/store/modules/user.ts`：`useUserStore()`（avatar 默认头像 + logout() 调 removeAccessToken），靠 auto-import `dirs:['src/store/modules']` 自动注册。
- 复制 `src/assets/images/default-avatar.jpg`（来自 jczz-data-analysis）。
- 新增 `src/layout/components/Navbar/UserDropDown.vue`（复用 SvgIcon + TipModal + useUserStore，菜单：清空缓存/退出登录）。
- 改 `src/layout/components/Navbar/index.vue`：引入 UserDropDown 并加入右侧个人中心槽位。
- 验证：`vue-tsc -b && vite build` 通过；auto-import.d.ts 已生成 `useUserStore`。
- 补 `el-dropdown` 样式：新增 `src/styles/element-plus/el-dropdown.scss`（`.el-dropdown .el-tooltip__trigger:focus-visible{outline:none}`），并在 `src/styles/index.scss` 按用户要求用相对路径 `@use './element-plus/el-dropdown.scss'` 引入（与文件内其余 `@use` 风格统一；用户明确偏好相对路径，非 `@/` 别名）。构建确认该规则已进入产物 css。
- 注：构建偶发 `[safe-delete] trash` 报错是 dist 清理环节环境问题，非代码问题；`rm -rf dist` 后重 build 即恢复。

## 从 jczz-data-analysis 迁移 ProPagination 到本项目
- 目标：把封装分页组件 ProPagination 完整迁移，保留原接口与功能（total / pageSizeList / layout / background / pagerCount / align + `v-model:current-page` / `v-model:page-size` + `@pagination` 事件；移动端响应式切 layout 与 pagerCount）。
- 依赖已齐备：`useAppStore()`（auto-import `dirs:['src/store/modules']`，app store 已有 `isMobile`）、`el-pagination`（ElementPlusResolver 按需）、`PropType`/`computed`（auto-import 'vue'）。无需新增 util/store。
- 新增 `src/components/ProPagination/index.vue`（原样复制 jczz 版）。
- 改 `src/plugins/modules/global-component.ts`：import ProPagination + `app.component('ProPagination', ProPagination)` 全局注册（与 SvgIcon 同模式，main.ts 经 setupPlugins 自动生效）。
- 改 `src/types/global/global-component.d.ts`：GlobalComponents 增加 `ProPagination` 类型声明（路径 `../components/ProPagination/index.vue`）。
- 验证：`vue-tsc -b && vite build` 通过；ProPagination 已进 `dist/js/index-*.js` 主包。
