# 2026-08-02

## 文档
- 新增 `docs/首屏加载动画教程.md`：总结白屏阶段 CSS 加载动画的实现（index.html 预置 `.app-loading` + `public/css/app-loading.css` + `main.ts` 的 `app.mount` 整体替换移除），含原理/文件职责/关键代码/定制/注意事项。用户要求精简、不加无关解释。
- 重写为 `docs/首屏加载动画.md`（功能说明版，去掉"教程"定位）：聚焦功能定位 + 实现要点 + 这么做的好处 + 对工程化的好处（关注点分离/FOUC 一致性/可复制标准实践/零运行时依赖），并删除旧的 `docs/首屏加载动画教程.md`。
- 抽离 `docs/动态标题.md`（功能说明版）：动态标题功能定位 + 实现要点（useDynamicTitle hook / App.vue 调用 / setting store / defaultSettings / settings 开关 / 路由 meta.title）+ 好处 + 工程化好处（关注点分离/配置中心化/易扩展/复用设置体系）；并在 `docs/前端工程化.md` 第 59 行把原接入说明改为指向新文档的引用。
- 抽离 `docs/ElementPlus按需引入.md`（功能说明版）：Element Plus 按需引入（自动导入）功能定位 + 实现要点（三插件互补：unplugin-vue-components + ElementPlusResolver / unplugin-auto-import + 同 resolver / unplugin-element-plus({useSource:true})；additionalData 亮色主题 + index.scss 暗黑 vars）+ 好处（体积可控/开发体验/主题统一）+ 工程化好处（确定性方案/主题与构建解耦/图标约定一致/标准实践）+ 注意事项（缺一不可、暗黑主色未统一、函数式 API 显式 import、SvgIcon 优先）；并在 `docs/前端工程化.md` 第 56 行加指向新文档的引用。
- 拆分：用户指出"按需引入"与"自定义主题"应分两篇。将 `docs/ElementPlus按需引入.md` 重写为只讲三插件自动导入（移除主题段落/主题相关表格行/注意事项），新建 `docs/ElementPlus自定义主题.md`（sass 变量覆盖 + 暗黑 css-vars + additionalData/index.scss 注入 + 好处 + 工程化好处 + 注意事项）；`docs/前端工程化.md` 第 56 行末尾引用改为同时指向两篇。
- 抽离 `docs/Vite插件独立模块设计.md`（功能说明版）：Vite 插件从 vite.config.ts 抽离到 build/plugins/ 独立模块（setupVitePlugins 汇总）+ 实现要点（index/auto-import/svg-icons/dist-compression + tsconfig.node 纳入 + nodenext 带 .ts）+ 好处（配置即装配/单文件单职责/可复用）+ 工程化好处（关注点分离/类型安全/解析确定/可复制范式）+ 注意事项；`docs/前端工程化.md` 第 25 行"Vite 插件抽离"条目末尾加引用。
- 抽离 `docs/设备自适应.md`（功能说明版）：设备自适应/响应式断点（单一 `html[data-device]` 信号 + CSS 覆盖范式）功能定位 + 实现要点（useResize / app.ts device / App.vue 启用 / 各页面 CSS 覆盖）+ 好处（断点集中/声明式响应/无重复监听）+ 工程化好处（统一范式/可复制/状态层协同）+ 注意事项；并在 `docs/前端工程化.md` 第 54 行同步阈值 768px→992px 且加引用。**顺带修代码**：`src/hooks/useResize.ts` 的 `MAX_MOBILE_WIDTH` 由 768 改为 992（原注释已写 992，代码与注释不一致，用户确认统一为 992）；全项目其余 768 均为 SVG 坐标 / dist 产物，与断点无关未动。
- 抽离 `docs/SvgIcon图标.md`（功能说明版）：SVG 图标方案（vite-plugin-svg-icons 运行时 sprite + 统一 SvgIcon 组件 + clean:svg 清理脚本）功能定位 + 实现要点（SvgIcon 组件 / svg-icons-plugin / main.ts virtual:svg-icons-register / scripts/svg-clean.ts / svg-icons 目录 / global-component.d.ts 类型）+ 好处（统一来源/运行时注入/主题友好/易扩展）+ 工程化好处（契合 AGENTS.md 图标规范/标准化流程/可复制范式）+ 注意事项（文件名即图标名、clean:svg、插槽优先、register 必引入）；并在 `docs/前端工程化.md` 第 57 行加引用。
- 文档分门别类：将 docs 根下 7 篇功能说明按主题归入 4 个子目录——`构建与工程化/`(Vite插件独立模块设计)、`UI组件/`(ElementPlus按需引入、ElementPlus自定义主题、SvgIcon图标)、`用户体验/`(首屏加载动画、动态标题)、`响应式/`(设备自适应)；`前端工程化.md` 留根作索引。同步更新所有交叉引用（前端工程化 6 处改子路径、子文档互引改相对路径、AGENTS.md 改 `../AGENTS.md`）；grep 验证无遗留旧 `docs/` 前缀与裸 `AGENTS.md`。
- 抽离三篇（用户指定：全局设置中心 / 主题切换 / 系统级水印）：`docs/构建与工程化/全局设置中心.md`（SystemSetting 类型+默认值 + useSettingStore merge 初始化 + saveSetting/resetSetting + system-setting.cache 领域封装；好处=集中默认值/配置中心化/持久化透明/即时延迟双模式；工程化好处=SSOT/Pinia 协同/解耦/可扩展/可复制；注意事项=saveSetting showTip 默认、reset 清缓存 reload、merge 顺序、主题保存时机、toRaw 快照）、`docs/用户体验/主题切换.md`（useTheme 统一逻辑层：切 html.dark 类+View Transitions 圆形扩散+防 FOUC+降级；与 ElementPlus自定义主题.md 区分 编译期 vs 运行时）、`docs/用户体验/系统级水印.md`（layout 根 el-watermark 包裹全站、content 受开关控制、font 随 isDark 联动）。同步精简 `前端工程化.md`：第 59 行拆为「全局设置中心」+「主题切换」两个指针条目，第 60 行末尾加设置中心引用，第 61 行「全局水印」精简为「系统级水印」指针。截止共 10 篇功能说明。
- 抽离 `docs/构建与工程化/开发环境反向代理.md`（功能说明版）：Vite dev server `server.proxy` 开发期跨域方案。实现要点（vite.config.ts proxy 配置[VITE_BASE_API 作匹配前缀/target=VITE_BASE_URL/changeOrigin/rewrite 剥离 /dev-api]、.env.development[VITE_BASE_URL=localhost:3000 + VITE_BASE_API=/dev-api/api]、.env.production[仅 VITE_BASE_API=/api 无代理]、request/index.ts baseURL 由 env 驱动、vite-env.d.ts 类型声明）+ 机制（浏览器同源→dev server→Node 转发）+ 好处（开发期零跨域/切环境换后端只动 .env/单变量合一/后端无公共路径时零配置即代理生效）+ 工程化好处（关注点分离/环境隔离/可复制范式/类型安全）+ 注意事项（代理仅 dev、VITE_BASE_URL 仅 dev、rewrite 只剥离 /dev-api、changeOrigin 必开、纯前端 mock 边界占位）。同步精简 `前端工程化.md` 第 37 行「开发期反向代理」内联描述为指针。截止共 11 篇功能说明。
- 补全反向代理文档优势：用户指出「后端无公共路径时（如 /user/list 而非 /api/user/list），`VITE_BASE_API` 写 `/dev-api` 去掉 /api 段，代理匹配键/baseURL/rewrite 三者同源派生、自动全局生效，无需改 vite.config/axios/rewrite」。在「好处」新增该条，并点明「三处同由 VITE_BASE_API 派生」的根因。
- 抽离 `docs/构建与工程化/构建文件拆分.md`（功能说明版）：构建产物分包策略（vite.config.ts build 段）。实现要点（codeSplitting.groups 把 vue/pinia/vue-router/axios/dayjs 拆成独立 chunk[priority 16→12]、chunkFileNames/entryFileNames=`js/[name]-[hash].js`、cssCodeSplit:true、chunkSizeWarningLimit=4096、assetsInlineLimit=4096）+ 机制（Rolldown 按正则归类、hash 驱动缓存失效）+ 好处（vendor/app 分离长缓存命中/多块并行下载/产物可观测/命名可预测）+ 工程化好处（关注点分离/可维护/可复制范式/与 gzip 协同）+ 注意事项（priority 大优先、仅拆稳定库、hash 不可去、与 gzip/dropConsole 是三件独立事、warningLimit 非硬上限）。同步精简 `前端工程化.md` 第 22 行 vendor 分包内联描述为指针。截止共 12 篇功能说明。
- 用户要求重写「构建文件拆分.md」：只谈拆分本身，移除 assetsInlineLimit/chunkSizeWarningLimit/gzip 协同/dropConsole 等旁支；好处聚焦「利用浏览器缓存机制（hash 稳定→强缓存命中、二次访问免重下）+ 减小用户侧有效传输体积（vendor 长效缓存、回访只下业务块）+ 首屏多块并行下载」。实现要点保留 codeSplitting.groups / chunkFileNames-entryFileNames / cssCodeSplit 三项。同步把 `前端工程化.md` 第 22 行指针「配套阈值」措辞改为「拆分收益与命名规则」。
- 「构建文件拆分.md」补充分拆收益：用户问还有其它好处吗。新增 5 条拆分本身独有收益——首屏按需加载（路由级代码分割）、单块体积可控降低主线程解析阻塞、变更影响范围收窄（精准失效）、可精细控制加载优先级（modulepreload/prefetch）、命名块便于 DevTools 定位（DX）。均不牵扯 gzip/dropConsole/阈值。
- 抽离 `docs/构建与工程化/构建期移除调试语句.md`（功能说明版）：构建期 Rolldown `minify.compress.dropConsole/dropDebugger` 剔除 console/debugger。实现要点（vite.config.ts minify.compress 两开关[默认启用、VITE_DROP_*!=='false']、.env.production 显式 true、.env 默认值语义）+ 机制（仅 vite build minify 阶段生效、dev 不 minify 故保留、是删除语句非拦截）+ 好处（产物干净防泄露/体积更小/杜绝 debugger 误触发/去无谓开销/行为可预期）+ 工程化好处（零运行时依赖/可配置可逆/标准能力可复制）+ 注意事项（仅生产生效/默认开需显式 false 才保留/勿依赖 console 做关键逻辑/与 sourcemap 无关/全量删除不分级别）。同步把 `前端工程化.md` 第 24 行内联描述精简为指针。截止共 13 篇功能说明。
- 「构建期移除调试语句.md」补好处：用户问还有其它好处吗。新增 2 条 distinct 收益——降低内存泄漏风险（console.log 持对象引用阻碍 GC）、满足生产就绪/审计合规（生产残留 console/debugger 是常见审计扣分项）。其余如移动端大对象日志开销属已写运行时开销子情形，不单列。好处现共 7 条。
- 「构建期移除调试语句.md」再补全好处：用户要求把缺失的好处补全。穷举后又补 3 条 distinct——减少错误监控/APM 噪声（Sentry 等只捕真实错误）、降低终端用户困惑/客诉风险（DevTools 满屏日志易误判 bug）、团队规范自动化/减轻 Review 负担（构建统一剔除避免漏删带上线）。其余（移动端开销、PII、CSP 等）均为已列项子情形不单列。好处现共 10 条。
- 抽离 `docs/构建与工程化/构建期Gzip预压缩.md`（功能说明版）：构建期 vite-plugin-compression2 对产物 gzip 预压缩。实现要点（dist-compression.ts 配置[algorithms gzip level 9 / threshold 10KB / deleteOriginalAssets false / include 文本类后缀 / logLevel silent]、index.ts 注册仅生产、dist 生成 .gz 兄弟文件）+ 机制（构建期生成 .gz、请求链路 gzip_static/CDN 命中直发、原文件兜底）+ 好处（传输体积降~61%/压缩前移服务端零实时开销/首屏 LCP-FCP 改善/保留原文件可灰度/阈值只压值得压/只压文本避免重复压缩/level9 最优/逐块压缩与分包协同/CDN 边缘友好/标准 HTTP 零运行时依赖）+ 工程化好处（构建运行解耦/可配置可演进/标准可复制）+ 注意事项（须服务端开 gzip_static 才生效/仅生产/双份致 dist 变大/与分包剔除三件独立/阈值算法可调）。同步把 `前端工程化.md` 第 23 行内联描述精简为指针，并把 `Vite插件独立模块设计.md` 第 14 行 dist-compression 引用指向新文档。截止共 14 篇功能说明。
- 「构建期Gzip预压缩.md」补好处：用户问还有遗漏吗。穷举后又补 3 条 distinct——降低带宽/托管成本（按量计费省费）、利好 SEO 排名（LCP 是排名因子）、高并发下延迟更稳定（免实时压缩 CPU 毛刺）。其余（TTFB、移动端流量、缓存友好）均为已列项子情形不单列。好处现共 13 条。
- 抽离 `docs/UI组件/全局组件注册.md`（功能说明版）：全局组件（SvgIcon）显式注册 + 手写类型声明，而非 unplugin-vue-components 目录扫描。实现要点（plugins/modules/global-component.ts 的 app.component('SvgIcon')、plugins/index.ts 的 app.use、main.ts setupPlugins 阶段、types/global/global-component.d.ts 手写 GlobalComponents.SvgIcon、auto-import-plugin.ts 的 registerAutoComponents dirs:[] 仅作 EP resolver）+ 机制（运行期注册/类型期手写声明/auto-components.d.ts 仅含 EP 组件不含 SvgIcon）+ 好处（可控可预期/类型与运行时一致/不污染自动生成/注册集中易扩展）+ 工程化好处（关注点分离/构建健壮/可复制范式）+ 注意事项（dirs 留空原因=避免目录全量变全局/命名冲突/不可预期、注册与类型同步、global-component.d.ts 手写非自动生成、仅 SvgIcon 走此通道）。同步把 `SvgIcon图标.md` 第 16 行「类型声明」明确为「手写」并指向新文档。截止共 15 篇功能说明。

## 工程化 Skill 调整
- 删除项目级 Skill `iterate-readme`（README 同步器）：用户明确「更新 readme.md 的那个删了不要了」。删除时安全删除包装器（genie-trash）因相对路径解析 bug 失败、fail-closed 未删；改用 `find -delete` + `rmdir` 强制删除，已确认 `.workbuddy/skills/` 下仅剩 add-global-component / git-commit-msg / path-alias。
- 新增项目级 Skill `iterate-docs`（`.workbuddy/skills/iterate-docs/SKILL.md`）：定位为「根据最新改动的代码完善迭代 docs/ 下的文档」，与已删的 iterate-readme 互补（后者管 README，本 skill 只管 docs、不碰 README）。含「代码改动→文档章节」映射表（src/styles·uno·svg-icons·EP·components·build·store/router·utils·hooks/useTheme·settings·layout·watermark·index.html·vite proxy·响应式·前端工程化总纲 各自对应文档）、八步流程（定范围→映射→核对→自检硬约束→先出方案→等确认→落地→二次核验）、并带 AGENTS.md 图标/@utils/纯前端约束与「代码是真相源」原则。触发语：同步文档/docs 迭代/扫描代码更新 docs/代码改了更新文档。
- **待办**：`README.md`（第 18、53 行）与 `docs/前端工程化.md`（第 74 行）仍残留 `iterate-readme` 引用（"4 个 Skill 含 iterate-readme"）。用户尚未确认是否清理，暂未改动这些文件。
- 清理残留引用（用户选 A）：`README.md` 第 18 行「4 个」→「3 个」、去 iterate-readme；第 53 行「四个」→「三个」、去 iterate-readme 描述；`docs/前端工程化.md` 第 74 行「4 个」→「3 个」、去 iterate-readme 及 README 配套说明、改为介绍 `iterate-docs`（负责根据代码改动迭代 docs）。grep 验证全仓无 iterate-readme 残留。
- 增强 `iterate-docs`（用户指出 docs 会持续增多，静态映射表会过期）：在第二步「映射」前新增 2.0「文档清单核对」运行时自检步——`find docs -name '*.md'` 列全量文档、校验映射目标路径是否仍存在（stale 提示改名/迁移）、列出全部文档作候选归属池、未命中时按目录名/标题关键词推断或交用户指定；映射表定位从「封闭清单」降级为「已知锚点种子」，不卡正确性、防 silent 漏更/死路径。
- 抽离两篇「功能说明版」文档（用户说"开始"后落地）：`docs/构建与工程化/路径别名.md`（`@/*` 别名：vite `resolve.alias`+tsconfig `paths` 双向同步、编译期重写零开销；好处 12 条：灭脆弱相对路径/重构友好/语义清晰/统一解析基准/类型安全/免 ./../ 歧义/按域组织/与 @/utils barrel 协同/零运行时开销/dev·build 一致/缩 import/可复制；工程化好处：关注点分离/可维护/类型构建解耦/可复制范式/约束即文档；注意双向同步缺一不可、import.meta.url 写法、paths 仅类型期）+ `docs/UI组件/统一消息反馈.md`（TipModal 统一反馈层：收口 ElMessage/ElMessageBox/ElNotification/ElLoading 四件套、统一"系统提示"标题与按钮文案、confirm/prompt 返回 `{confirm,cancel}` 免 try-catch、loading 单例；好处 11 条：单一入口禁散装 import/统一话术/结构化返回免 catch/类型安全/集中治理/与按需引入解耦/loading 单例防叠加/全局复用/缩写调用/替换成本低/可复制；注意函数式 API 样式靠 unplugin-element-plus、cancel 由 reject 归一、loading 须配对 hideLoading、抽象类仅类型层）+ 同步 `docs/前端工程化.md`：第 35 行路径别名内联描述改指针、第 56 行 Element Plus 段后新增「统一消息反馈（TipModal）」指针（第 58 行）。截止共 17 篇功能说明。
