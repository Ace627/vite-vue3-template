# 2026-07-20

- 理解项目现状：`vite-vue3-template` 是 `pnpm create vite`（Vue + TS）原始产物，版本较新（Vite8 / Vue3.5 / TS6）。未 `pnpm install`、未 `git init`、`src/` 仍为 demo。
- 产出「工程化模板分层蓝图」（5 层能力缺口），确认后续改造方向。用户表示会自行指挥下一步，无需我主动出方案落地。
- 按指令将 index.html 的 `<html lang="en">` 改为 `zh-CN`（中文本地化第一步）。
- 新增 index.html 的 `<meta name="description">` 占位符 + vite.config.ts `transformIndexHtml` 自动注入；以 `package.json.description` 为单一真源，两处描述自动同步、永不漂移。
- 清理 create vite 的 demo 脏数据：删除 `HelloWorld.vue`、`style.css` 及 `src/assets/` 下三个 demo 素材，并清除 `App.vue`/`main.ts` 的引用；`src/` 现为干净空壳起步。
- 安装 `sass -D`（1.101.0）；新建 `src/styles/index.scss` 全局样式入口，并在 `main.ts` 引入。
- 按指令将 `src/styles/index.scss` 清空为空白（移除预置 reset），保持空文件，后续再填内容。
- `src/main.ts` 改为 `bootstrap()` 异步引导模式（仅依赖现有 vue/sass/App）。按用户「没有的就先不加」的指示，**删除了**此前为补引用而建的 4 个 stub 文件及空目录；后用户进一步要求「连 TODO 注释也不要」，已将 `main.ts` 精简为仅 `createApp` → `mount` → 打印完成，无任何对未安装模块的引用或注释。
- 新增 `public/css/reset.css`（box-sizing 重置、旧版浏览器新布局元素兼容、html/body/#app 全高、移动端点击高亮去除等），并在 `index.html` 的 `<head>` 内用 `<link rel="stylesheet" href="/css/reset.css" />` 引入。特意不走 `index.scss` 引入，避免打包后加载时序靠后导致样式跳动/FOUC。
- 按指令创建项目级 Skill `git-commit-msg`（`.workbuddy/skills/git-commit-msg/SKILL.md`）：基于 `git diff` 实际改动生成 Conventional Commits 中文提交信息（格式 `<type>: <简述>`），只输出 message 不擅自提交；暂存区含 `.workbuddy/` 等工具目录时提醒用户而非擅自处理。
- 用户进一步明确：该 Skill 输出**只要单行** `<type>: <简述>`，不要下面的分条 body。已回改 SKILL.md（message 格式、示例、注意 三处均改为仅单行输出）。
- 用户再次升级 `git-commit-msg` 流程：每次给出 **5 条候选单行 msg** → 让用户选一条 → 选完**再单独询问是否提交到暂存区**（`git commit`，写操作需用户确认；无暂存改动时先确认是否 `git add`）。已重写 SKILL.md 体现「5 候选 + 选择 + 二步确认提交」流程。
- 用户再次调整 `git-commit-msg` 提交交互：选定 msg 后的第二步由「是否提交」改为三选一——**提交到暂存区**（仅 `git commit`）/ **提交并推送**（`git commit` + `git push`）/ **跳过**（不操作）。已更新 SKILL.md 第 5 步、示例、注意 三处。
- 实测 `git-commit-msg` 流程：生成 5 候选 → 用户选 1（`chore: 初始化前端工程化模板基础骨架`）→ 选「提交到暂存区」。**（注：此前提到的 de646d2 等 commit 已被用户手动回撤，仓库一度回到无 commit 状态；下方为实际生效的首次提交。）**
- 实际首次提交：重新执行 `git commit -m "chore: 初始化前端工程化模板基础骨架"`，将全部 14 个 untracked 文件（含 `.workbuddy/`）作为本地 master 的 root-commit 入库，工作区干净，**未推送**。注意：该自定义项目 Skill 无法通过 Skill 工具按名加载（报错 "Can not find skill"），需直接按 SKILL.md 文件内容执行。
- 新增仓库根 `.gitattributes`（`* text=auto eol=lf`），统一文本文件行尾为 LF，消除 Windows 下 `core.autocrlf=true` 导致的 `LF will be replaced by CRLF` 告警，并根除 `checkout`/`reset` 时将 LF 源文件改写成 CRLF 而弄脏工作区的隐患。提交时仅纳入该文件与本记忆，不卷入用户其他未提交改动（如 `src/main.ts`）。
- 实现路径别名 `@/*` → `src/*`：`tsconfig.app.json` 的 `compilerOptions.paths` 增加 `"@/*": ["./src/*"]`；`vite.config.ts` 的 `defineConfig` 增加 `resolve.alias['@'] = fileURLToPath(new URL('./src', import.meta.url))`（`fileURLToPath` 已 import）。已跑 `pnpm build` 验证（`vue-tsc -b` + `vite build` 均通过，exit 0），两个配置均可正常解析。约定：TS 与 Vite 两处必须同步，导入统一用 `@/`。
- 为 `tsconfig.app.json` 的其余配置项补齐与 `paths` 同款的 `/** @description @note */` 注释（`tsBuildInfoFile`、`types`、`allowArbitraryExtensions`、`noUnusedLocals`、`noUnusedParameters`、`erasableSyntaxOnly`、`noFallthroughCasesInSwitch`、`extends`、`include` 等），统一可读性。`vue-tsc -b` 验证通过（exit 0）。
- 沉淀可复用流程为项目级 Skill `path-alias`（`.workbuddy/skills/path-alias/SKILL.md`）：Vite+Vue+TS 项目配置 `@/*` 别名的完整步骤——tsconfig.app.json 加 `compilerOptions.paths["@/*"]` + vite.config.ts 加 `resolve.alias['@']=fileURLToPath(new URL('./src',import.meta.url))`、强调 TS/Vite 两处同步、ESM 无 `__dirname` 需用 `fileURLToPath(new URL(...))`、以及 `pnpm build` 验证。属项目级，无法经 Skill 工具按名加载，需按 SKILL.md 手动执行。
- 更新 `README.md`：由 create-vite 默认英文 5 行改为中文模板说明，新增「内置 SKILL（项目级）」章节标注 `git-commit-msg` 与 `path-alias` 两个 Skill 的路径/作用/用法，并补充「快速开始」与「已落地的工程化约定」小节。同步将 `path-alias` 补入 MEMORY.md 的「项目内 Skill」清单。
- 更新 `git-commit-msg` Skill：将用户习惯说法「提交代码」加入触发词（frontmatter description、触发场景、示例输入三处均补充，并标注其为更常用说法）；同步把 README 中该 Skill 的触发语改为「提交代码 / 生成 git 的 msg / 提交说明」。
- 提交并推送：通过 `git-commit-msg` 流程（选 1）→ 选「提交并推送」。执行 `git commit -m "chore: 配置 @ 路径别名并完善内置 SKILL 文档"` 后将 6 个修改文件 + `path-alias` Skill 目录（含本次记忆更新）一并 `git push` 至 origin/master。提交内容覆盖：`@/*` 别名配置 + tsconfig 注释、`git-commit-msg` 触发词、README 内置 SKILL 说明、新增 `path-alias` Skill。
