# 2026-08-03 工作日志

## 修正 git-commit-msg 技能（octal 路径误读纠偏）
- 在 `D:\Project\vite-vue3-template\.workbuddy\skills\git-commit-msg\SKILL.md` 的「历史纠偏」段新增一条：git 对中文路径用 octal 转义显示，禁止凭肉眼解码下结论，须用 `git ls-files -s` / `git cat-file -p <blob>` / `git -c core.quotePath=false ls-files` 核实真实路径与内容。
- 背景：2026-08-02 提交时曾把 `构建与工程化/路径别名` 误读为 `知识库与工程化/零冗余原则`，引发误判并多走一轮 `reset --soft` 重提交；事后查明并无错误文件，最终以 `c379fb4` 正常提交 4 个正确文件。
- 当前状态：该 SKILL.md 改动**尚未提交**（用户确认「加进去」仅指写入文件）。如需纳入版本管理，走 git-commit-msg 流程本地提交即可。

## 回撤未提交的「SYSTEM_CONSTANT 常量体系」改动
- 背景：此前（非本次会话）在仓库引入了一套未提交的常量体系——新增 `src/common/constant/system.ts`（`SYSTEM_CONSTANT` 含 DEVICE_*/THEME_* + `DeviceType`/`ThemeType`）、`src/common/index.ts`，并把 `app.ts`/`useResize.ts` 改为引用 `@/common` 常量（原硬编码 'desktop'/'mobile'）。其中 `THEME_*`/`ThemeType` 属死代码（0 处引用）。
- 用户决策：暂不整理类型/常量，回撤本次常量改动、用回原硬编码。
- 执行：`git rm -f` 删除两个新增文件 + `git checkout HEAD --` 恢复 `app.ts`/`useResize.ts`，`grep` 确认无 `@/common` 残留。回撤后 `src/` 改动清空，仅剩 `.workbuddy` 下的 memory/skill 记录。
- 结论：项目回到讨论前状态（theme 仍用 defaultSettings.ts 的 `'light'|'dark'` 字面量、useTheme 硬编码 'dark'；device 仍硬编码 'desktop'/'mobile'）。类型/常量收口议题暂缓。

## 新增工程化文档：目录结构说明
- 产出：`docs/目录结构.md`，逐目录、逐文件注释项目结构（简单明了导向）。
- 处理方式：`src/assets/svg-icons/` 下 74 个图标文件按「目录级注释 + 命名约定」合并说明（避免逐条噪音）；其余文件逐条注释。
- 覆盖：根目录、build/、docs/、public/、scripts/、src/（含 styles/types/utils/request/views 等子目录）。
