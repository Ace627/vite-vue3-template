## 2026-08-13

### 提交：多标签页面签（TagsView）功能落地
- commit `13d0511` | message：`feat: 新增多标签页面签及路由重定向`
- 15 文件，+564 / -15
- 新增：TagsView 组件、tags-view store、tags-view cache 持久化、redirect 刷新页、router.helper 路径解析
- 配套：defaultSettings 加 `showTagsView`/`showTagsViewIcon` 开关；SettingPanel 放开两个开关；vue-router.d.ts 类型增强（affix/activeMenu/keepAlive/alwaysShow）；variables.scss 加 `--el-tags-view-*` 变量（明暗主题）；reset.css 补充默认 margin/padding 清零与 li 去符号；AppMain 按页签高度调整；SidebarItem 注释 analysis 新窗口打开逻辑
- 待确认：SidebarItem 注释掉 `analysis` 走新窗口打开、改为页签内 `router.push`，该行为变更需用户确认是否符合预期
